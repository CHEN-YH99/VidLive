# VidLive - 用户需求文档

## 文档说明

- **版本**：v1.1（竞品调研补充版）
- **更新日期**：2026年6月3日
- **产品目标**：MVP 阶段需求定义
- **调研范围**：Apple 官方 Live Photo 行为、在线 Live Photo 转换工具、iOS Live Wallpaper 制作类 App

---

## 1. 产品定位

VidLive 是一个面向 iPhone 用户和内容创作者的 Web 工具，用于把视频、GIF 或图片素材转换为可用于相册、锁屏壁纸和社交分享的 Live Photo 相关格式。

### 一句话定位

**不用安装 App，尽量不上传私人素材，也能把短视频快速做成 iPhone 可用的实况照片或动态壁纸素材。**

### 差异化方向

1. **Web 优先**：用户打开网页即可处理，不把下载 App 作为第一步。
2. **隐私优先**：短视频优先本地处理；需要云端处理时明确告知上传、保存时长和删除规则。
3. **锁屏可用优先**：不只生成文件，还要告诉用户能不能作为 iOS 锁屏 Live Wallpaper 使用。
4. **结果可验证**：每次导出都给出兼容性提示、失败原因和下一步操作，而不是只丢一个 ZIP 包给用户自己悟，悟到天光都没用。

---

## 2. 竞品与平台调研结论

| 来源 | 观察到的能力 | 对 VidLive 的需求启发 |
| --- | --- | --- |
| [Apple - Edit Live Photos on iPhone](https://support.apple.com/guide/iphone/edit-live-photos-iphd8dbb3291/ios) | Apple 相册支持修改关键照片、裁剪 Live Photo、静音、Loop、Bounce、Long Exposure、保存为视频。 | MVP 必须覆盖关键照片、裁剪、静音和视频/GIF 降级导出；这些不是高级功能，是用户心智里的基础操作。 |
| [Apple - Set a Live Photo as Lock Screen wallpaper](https://support.apple.com/en-us/120734) | iOS 17 及以上支持把 Live Photo 作为锁屏壁纸，并可在唤醒设备时播放。 | 需求里必须单独定义“锁屏壁纸模式”，不能只说“生成 Live Photo”。 |
| [LivePhoto.online](https://livephoto.online/) | 强调浏览器内本地处理、无上传、无账号、无水印，并提供多格式导出、时间轴裁剪、FPS、缩放、比例和背景控制。 | VidLive 需要补“本地隐私模式”和“无账号快速流程”，否则用户上传私人视频的心理门槛很高。 |
| [LivePhoto.video](https://livephoto.video/) | 覆盖 Video to LivePhoto、Image to LivePhoto、LivePhoto to GIF、GIF to LivePhoto、AI 图片动效等工具。 | 产品边界可扩展为 Live Photo 工具箱，但 MVP 只能先做 Video/GIF to Live Photo，别一口吞到打嗝。 |
| [intoLive App Store 页面](https://apps.apple.com/us/app/intolive-live-wallpapers/id1061859052) | 支持社交比例、多素材合成、草稿、撤销/重做、文字贴纸、音乐、滤镜、转场、速度调整、SD/HD/FHD/4K、24/30/60 FPS。 | V1/V2 可以向轻量编辑器扩展；MVP 不做完整视频编辑器，只保留裁剪、比例、封面帧、静音。 |
| [intoLive Live Wallpaper 指南](https://intolive.zendesk.com/hc/en-us/articles/49753923778457-How-to-make-Live-Wallpaper) | 对 iOS 17+ 锁屏壁纸给出 1-2 秒、60 FPS、减少剧烈运动、注意中段播放等建议。 | 锁屏模式需要预设参数和风险提示；相关兼容结论必须通过真机验证，不能当铁律写死。 |
| [VideoToLive App Store 页面](https://apps.apple.com/us/app/videotolive/id1525653194) | 主打视频/照片转 Live Photo、自定义关键时间、自定义关键照片、裁剪、旋转、翻转。 | 手动关键帧、裁剪、旋转、翻转应进入 MVP 或 V1 早期，不应被 AI 推荐功能盖过去。 |

### 现有文档主要不足

1. **缺少竞品约束**：没有说明同类产品为什么都强调本地处理、关键帧、比例、锁屏兼容。
2. **MVP 范围偏飘**：上传 500MB、断点续传、AI 推荐、云存储、支付、社交直传同时出现，像过年摆盘，热闹但不知道先吃哪碟。
3. **没有锁屏壁纸专属需求**：用户真正常见诉求是“能不能设为 iPhone 锁屏动态壁纸”，这和普通 Live Photo 导出不是一回事。
4. **缺少保存路径设计**：Web 端如何保存为 Live Photo、是否需要 Shortcuts、AirDrop、ZIP、MOV，都没有说清。
5. **隐私信任不足**：私人视频上传到服务器是高敏操作，必须有本地模式、上传告知、自动删除和手动删除。
6. **验收标准过粗**：只写成功率和处理时间，缺少设备兼容、导出文件可导入、失败提示、无上传验证等标准。

---

## 3. 目标用户与核心场景

### 3.1 目标用户

| 用户类型 | 主要需求 | 优先级 |
| --- | --- | --- |
| iPhone 个性化用户 | 把短视频、动漫片段、旅行视频做成锁屏 Live Wallpaper。 | P0 |
| 内容创作者 | 把视频片段转换为 Live Photo、GIF、MP4，便于多平台发布。 | P1 |
| 设计师/运营 | 快速制作动态素材、活动图、演示素材。 | P1 |
| 批量处理用户 | 多个素材批量生成统一规格文件。 | P2 |

### 3.2 核心场景

1. 用户在手机 Safari 打开 VidLive，选择一个 5 秒视频，裁剪为 2 秒锁屏模式，选择封面帧，导出并按指引保存到相册。
2. 用户在桌面浏览器拖入 MP4/GIF，本地裁剪、调整比例和背景色，导出 Live Photo 相关文件或 GIF/MP4。
3. 用户的视频过大或浏览器不支持本地转码，系统提示切换云端处理，并说明上传范围、保存时长和删除方式。
4. 用户导出的 Live Photo 不能作为锁屏壁纸播放，系统提供原因排查：时长、帧率、运动幅度、iOS 版本、保存方式、Live 开关。

---

## 4. 产品目标与非目标

### 4.1 MVP 目标

1. 支持用户在不登录的情况下完成一次短视频/GIF 到 Live Photo 相关格式的转换。
2. 支持本地处理小文件，降低用户对隐私和上传的顾虑。
3. 提供两类导出预设：**标准 Live Photo** 和 **iOS 锁屏壁纸优先**。
4. 提供清晰的保存/导入指引，让用户知道下一步怎么把结果放进 iPhone 相册或锁屏。
5. 建立真机兼容测试矩阵，记录 iOS 17+ 主要版本和机型的成功率。

### 4.2 MVP 非目标

1. 不做完整视频剪辑器。
2. 不做社交平台直传。
3. 不做批量处理。
4. 不做账号体系、支付体系和云端历史记录，除非本地模式验证失败。
5. 不把 AI 智能选帧作为首发核心路径；AI 可以锦上添花，不能雪中送炭都没送到就开始卖花。

---

## 5. 用户故事

| ID | 用户故事 | 优先级 | 验收摘要 |
| --- | --- | --- | --- |
| US-01 | 作为 iPhone 用户，我想上传一个短视频并快速生成 Live Photo，以便保存到相册。 | P0 | 无登录完成上传/处理/导出，失败时有明确提示。 |
| US-02 | 作为锁屏壁纸用户，我想选择“锁屏模式”，让输出更可能在 iOS 17+ 锁屏播放。 | P0 | 自动使用短时长、关键帧居中、竖屏比例和兼容提示。 |
| US-03 | 作为隐私敏感用户，我想知道视频是否上传服务器。 | P0 | 本地模式明确显示“本地处理”；云端模式必须二次确认。 |
| US-04 | 作为创作者，我想手动裁剪视频片段和选择封面帧。 | P0 | 时间轴可拖动，支持起止点和关键帧选择。 |
| US-05 | 作为普通用户，我想导出后知道如何保存到 iPhone。 | P0 | 根据设备给出 iPhone Safari、桌面、AirDrop/Shortcuts 等指引。 |
| US-06 | 作为创作者，我想导出 GIF/MP4/WebP 兜底格式。 | P1 | Live Photo 之外可选择社交友好的降级格式。 |
| US-07 | 作为运营用户，我想一次处理多个视频。 | P2 | 批量队列、统一参数、打包下载。 |
| US-08 | 作为付费用户，我想处理更大文件和 4K 输出。 | P2 | 登录后可使用云端队列、4K、历史记录。 |

---

## 6. 功能需求

### 6.1 素材导入

#### P0：基础导入

- 支持 MP4、MOV、GIF。
- 单文件本地处理建议上限：100MB。
- 视频建议时长：1-30 秒；超出时必须引导用户裁剪。
- 自动读取并展示：时长、分辨率、帧率、文件大小、是否含音频、方向信息。
- 对不支持的编码给出可理解提示，例如“当前浏览器无法解码 HEVC，请改用云端处理或转换为 H.264 MP4”。

#### P1：增强导入

- 支持 WebP 动图。
- 支持图片生成轻微动态效果。
- 支持从 Live Photo 相关文件导入并转 GIF/MP4。

### 6.2 本地处理与云端处理

#### 本地处理模式（P0）

- 默认优先使用浏览器本地处理。
- 页面必须明确展示：素材不会上传服务器。
- 本地处理过程中显示阶段进度：解析、裁剪、编码、打包。
- 本地处理失败时给出原因，并允许切换云端处理。

#### 云端处理模式（P1）

- 仅在文件过大、浏览器不支持或用户主动选择时启用。
- 启用前必须告知：
  - 文件会上传服务器；
  - 默认保存时长；
  - 用户可手动删除；
  - 分享链接有效期。
- 云端临时文件默认 24 小时删除；登录用户可选择保留 7 天。
- 云端处理需要内容安全策略，至少限制公开分享链接的违规内容传播；本地处理不做内容扫描。

### 6.3 裁剪、关键帧与预览

#### P0

- 提供时间轴预览，可选择起点、终点和关键帧。
- 默认关键帧为片段中点。
- 支持逐帧或 0.1 秒级调整。
- 支持静音开关。
- 支持原始比例、9:16、1:1、4:5、16:9。
- 比例不匹配时支持两种策略：
  - 裁切填满；
  - 保留完整画面并补背景色。
- 导出前必须预览静态封面和动态片段。

#### P1

- 支持旋转、翻转。
- 支持播放速度调整。
- 支持亮度、对比度、饱和度基础调节。
- 支持 AI 推荐 3 个候选关键帧，评分依据包括清晰度、人脸、运动稳定性。

### 6.4 导出预设

#### 标准 Live Photo 预设（P0）

- 目标：用于相册保存、分享、后续导入。
- 推荐时长：3 秒。
- 关键帧默认居中。
- 输出内容：
  - 静态图：JPEG，后续可支持 HEIC；
  - 动态片段：MOV；
  - 打包文件：ZIP；
  - 辅助导出：MP4/GIF。

#### iOS 锁屏壁纸预设（P0）

- 目标：提高 iOS 17+ 锁屏播放成功率。
- 推荐时长：1-2 秒，具体以真机验证结果为准。
- 默认竖屏 9:16。
- 关键帧默认选择视频中段。
- 目标帧率：优先 60 FPS；源素材不足时提示可能不够顺滑。
- 提醒用户避免剧烈转场、闪屏、过快移动。
- 导出页必须展示“锁屏设置步骤”和“Motion not supported/不支持动态播放”的排查项。

#### 社交兜底预设（P1）

- GIF：适合聊天和网页预览。
- MP4：适合小红书、微博、抖音素材再编辑。
- WebP：适合网页和产品展示。

### 6.5 保存与导入指引

#### iPhone Safari

- 导出后显示适合手机端的保存路径。
- 若 Web 端无法直接写入系统 Live Photo，相应页面必须引导用户通过 Shortcuts、AirDrop 或 App 内保存方案完成。
- 指引文案必须区分：
  - 保存到相册；
  - 设置为锁屏；
  - 导出为 GIF/MP4 分享。

#### 桌面浏览器

- 支持下载 ZIP。
- 支持下载 MOV/MP4/GIF。
- 支持二维码或短链接发送到手机继续保存。

### 6.6 失败处理

系统必须识别并提示以下失败类型：

1. 文件过大。
2. 编码不支持。
3. 浏览器内存不足。
4. 本地转码失败。
5. 生成成功但无法作为锁屏动态壁纸。
6. 用户取消处理。
7. 云端任务超时。
8. 分享链接过期。

失败提示必须包含下一步建议，例如压缩文件、缩短时长、切换预设、改用云端、下载 MP4 兜底。

---

## 7. 非功能性需求

### 7.1 性能

| 指标 | MVP 目标 |
| --- | --- |
| 首屏可交互 | 桌面和移动端均小于 3 秒 |
| 本地解析 100MB 内视频 | P75 小于 10 秒 |
| 本地转换 10 秒 1080p 视频 | P75 小于 60 秒 |
| 云端转换 30 秒 1080p 视频 | P75 小于 30 秒 |
| 导出包生成 | P75 小于 10 秒 |

> 原文档“30秒视频 ≤ 10秒处理时间”对 Web MVP 偏乐观，尤其本地 FFmpeg/WASM 和移动端性能会波动。先把指标写成能验证、能优化的目标，别一上来就许愿。

### 7.2 兼容性

- 浏览器：Safari、Chrome、Edge 最新两个大版本。
- 设备：iPhone Safari 必须重点验证。
- iOS：重点验证 iOS 17 及以上主要版本。
- 输出：
  - 标准预设：能被 iPhone 相册识别为可用动态素材；
  - 锁屏预设：能在测试设备上设置为锁屏并播放，记录失败机型和失败原因；
  - 兜底格式：GIF/MP4 可在主流社交平台上传或预览。

### 7.3 隐私与安全

- 本地模式不得上传原始素材、缩略图或生成文件。
- 云端模式使用 HTTPS 和临时签名上传。
- 用户可在导出页手动删除云端文件。
- 云端文件默认 24 小时删除；分享链接最长 7 天。
- 不保留 EXIF 中可能暴露位置的信息，除非用户明确选择保留。
- 对公开分享文件增加滥用举报和下架机制。

### 7.4 可用性

- 首次用户必须能在 3 分钟内完成一次导出。
- 每一步只展示当前必要设置，高级参数收进“更多设置”。
- 移动端优先优化：上传、时间轴拖动、预览和下载按钮必须适合单手操作。
- 所有专业词要有解释，例如 FPS、关键帧、比例、锁屏模式。

---

## 8. MVP 范围

### Must Have

- MP4/MOV/GIF 导入。
- 本地处理模式。
- 手动裁剪起止点。
- 手动选择关键帧。
- 标准 Live Photo 预设。
- iOS 锁屏壁纸预设。
- ZIP、MOV、MP4、GIF 至少两类导出，其中 Live Photo 相关导出为核心。
- 手机端保存/设置指引。
- 明确隐私提示和失败原因。
- 基础真机兼容测试记录。

### Should Have

- 云端处理模式。
- 旋转、翻转。
- 9:16、1:1、4:5、16:9 预设比例。
- 背景色填充。
- 二维码发送到手机。
- 处理进度和预计剩余时间。

### Could Have

- AI 关键帧推荐。
- WebP 导出。
- 亮度/对比度/饱和度。
- 播放速度调整。
- 登录后历史记录。

### Won't Have in MVP

- 批量处理。
- 支付订阅。
- 社交平台直传。
- 模板市场。
- 完整视频编辑器。
- 背景音乐库。

---

## 9. 用户流程

```text
用户访问 VidLive
    ↓
选择处理模式
    ├─ 本地处理（默认）
    └─ 云端处理（文件过大或用户选择）
    ↓
导入 MP4/MOV/GIF
    ↓
系统解析素材并展示时长、分辨率、帧率、方向、音频
    ↓
选择导出预设
    ├─ 标准 Live Photo
    ├─ iOS 锁屏壁纸
    └─ GIF/MP4 兜底
    ↓
裁剪片段 + 选择关键帧 + 调整比例/静音
    ↓
预览静态封面和动态片段
    ↓
生成文件
    ↓
下载/保存指引/二维码发送到手机
    ↓
用户反馈是否成功保存或设置锁屏
```

---

## 10. 页面与交互需求

### 10.1 首页即工具页

- 首页直接展示上传区域，不做营销型落地页。
- 上传区域旁边只保留必要说明：支持格式、隐私模式、推荐时长。
- 不登录也能开始。

### 10.2 编辑页

- 主区域为视频预览。
- 下方为时间轴和关键帧选择。
- 右侧或底部为导出预设、比例、静音、质量。
- 移动端使用底部工具栏和抽屉式参数面板。

### 10.3 导出页

- 显示生成结果预览。
- 提供下载按钮。
- 根据设备显示保存步骤。
- 展示兼容性提示：
  - 标准 Live Photo；
  - 锁屏壁纸；
  - 社交分享。
- 提供“无法播放？”排查入口。

### 10.4 失败页/失败状态

- 不允许只显示“生成失败”。
- 必须展示失败类型、可能原因和下一步按钮。
- 例：
  - “浏览器内存不足，建议缩短到 2 秒或切换云端处理。”
  - “该文件是 HEVC 编码，当前浏览器无法本地解码，建议使用云端处理。”
  - “锁屏未播放，建议使用 1-2 秒片段、60 FPS 源素材并确认 Live 开关已开启。”

---

## 11. 商业化策略

### 免费版

- 无登录本地处理。
- 不加水印。
- 单文件建议上限 100MB。
- 基础导出：Live Photo 相关文件、MP4/GIF。
- 每日云端处理次数限制。

### Pro 版

- 云端大文件处理。
- 4K 输出。
- 批量处理。
- 更长分享链接有效期。
- 历史记录。
- 高级编辑和 AI 关键帧。
- 优先队列。

> 竞品普遍把无广告、无限导出、高级编辑、4K、批量作为付费点。VidLive 的免费版不建议加水印，因为“无水印 + 隐私”是 Web 工具建立信任的关键卖点。

---

## 12. 成功指标

| 指标 | MVP 目标 |
| --- | --- |
| 首次导出完成率 | ≥ 70% |
| 标准预设导出成功率 | ≥ 95% |
| 锁屏预设真机播放成功率 | ≥ 80%，并记录失败矩阵 |
| 上传/处理前退出率 | ≤ 35% |
| 本地模式使用占比 | ≥ 60% |
| 用户主动切换云端处理率 | ≥ 10% |
| 平均首次导出时长 | ≤ 3 分钟 |
| 失败提示后重试成功率 | ≥ 40% |

---

## 13. 验收标准

### 13.1 功能验收

- 用户无需登录即可完成一次 MP4 到 Live Photo 相关格式的导出。
- 用户可在时间轴上选择起点、终点和关键帧。
- 用户可选择标准预设和锁屏预设。
- 导出页在 iPhone 和桌面浏览器上展示不同保存指引。
- 本地模式下，原始素材不得发送到业务服务器。
- 云端模式下，用户必须先看到上传和删除规则。

### 13.2 兼容验收

- 至少使用 3 台不同 iPhone 真机测试。
- 至少覆盖 iOS 17 及以上两个主要版本；上线前以当时 Apple 当前版本重新确认。
- 标准预设结果可在相册中正常预览动态效果。
- 锁屏预设结果可设置为锁屏，并记录是否自动播放。
- 失败结果必须能定位到具体原因。

### 13.3 隐私验收

- 本地模式抓包确认不上传视频、GIF、缩略图和导出文件。
- 云端文件到期后自动删除。
- 用户点击删除后，下载链接立即失效。
- 隐私说明在用户上传前可见。

---

## 14. 风险与待验证问题

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Web 端保存为真正 Live Photo 的能力受 iOS 限制 | 用户导出后不会保存，体验断裂 | MVP 必须实测 Shortcuts、AirDrop、ZIP、MOV 等路径，选最稳定方案。 |
| iOS 锁屏动态壁纸兼容规则不透明 | 生成成功但锁屏不播放 | 建真机矩阵，提供锁屏专用预设和排查指引。 |
| 本地 FFmpeg/WASM 性能不稳定 | 移动端转换慢或失败 | 设置 100MB 本地上限，失败后引导云端处理。 |
| 用户隐私顾虑高 | 上传转化低 | 默认本地模式，云端模式二次确认和可删除。 |
| AI 选帧投入过早 | 耗时高但价值不稳定 | MVP 先做手动关键帧，AI 放 P1/P2。 |
| 500MB 云端处理成本高 | 成本不可控 | 云端能力放到 Pro 或限额使用。 |

---

## 15. 路线图

### Phase 0：技术与兼容验证（1-2周）

- 验证 Web 本地转码能力。
- 验证 iPhone 保存路径。
- 验证标准 Live Photo 和锁屏预设在真机上的成功率。
- 确定最终导出文件组合。

### Phase 1：MVP（2-4周）

- 本地处理 MP4/MOV/GIF。
- 时间轴裁剪和关键帧选择。
- 标准 Live Photo 预设。
- iOS 锁屏壁纸预设。
- 导出页和保存指引。
- 错误诊断和隐私提示。

### Phase 2：产品化（1-2个月）

- 云端处理。
- 大文件上传。
- 旋转、翻转、比例、背景色。
- 二维码发送到手机。
- 兼容测试面板和用户反馈。

### Phase 3：商业化（3-4个月）

- 用户系统。
- 付费订阅。
- 批量处理。
- 4K 输出。
- 历史记录。
- AI 关键帧。

### Phase 4：扩展（6个月+）

- Live Photo 工具箱：Live Photo to GIF/MP4、Image to Live Photo、GIF to Live Photo。
- 桌面客户端或轻量 iOS App。
- API 接口。
- 模板和创作素材库。

---

## 16. 参考链接

- [Apple - Edit Live Photos on iPhone](https://support.apple.com/guide/iphone/edit-live-photos-iphd8dbb3291/ios)
- [Apple - Set a Live Photo as your Lock Screen wallpaper on your iPhone](https://support.apple.com/en-us/120734)
- [LivePhoto.online](https://livephoto.online/)
- [LivePhoto.video](https://livephoto.video/)
- [intoLive - App Store](https://apps.apple.com/us/app/intolive-live-wallpapers/id1061859052)
- [intoLive - How to make Live Wallpaper](https://intolive.zendesk.com/hc/en-us/articles/49753923778457-How-to-make-Live-Wallpaper)
- [VideoToLive - App Store](https://apps.apple.com/us/app/videotolive/id1525653194)

---

**负责人**：待定
