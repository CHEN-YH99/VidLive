# VidLive - 隐私测试记录

## 文档说明

- **来源章节**：`开发周期文档.md` 11.3 隐私测试
- **执行日期**：2026年6月4日
- **自动检查入口**：`pnpm run privacy:check`
- **严格验收入口**：`pnpm run privacy:check:strict`

> **隐私测试手动验收状态**：待补证据

当前记录用于把 11.3 的隐私要求落成可重复检查入口。自动检查只能证明本地导出代码没有网络调用、云端上传前有确认状态、云端任务有过期和删除链路；抓包和链接失效仍必须真实验证。

---

## 1. 隐私测试状态

| 测试项 | 自动覆盖 | 必须补齐的真实证据 | 当前状态 |
| --- | --- | --- | --- |
| 本地模式抓包确认不上传原始素材 | 本地导出函数无 `fetch`、`XMLHttpRequest`、`axios`、`FormData` | 浏览器 DevTools/代理抓包记录，确认原始 MP4/MOV/GIF 未出站 | 待补证据 |
| 本地模式不上传缩略图 | 封面帧由本地 `<video>` + `<canvas>` + `toDataURL` 生成，检查器无网络调用 | 抓包记录，确认 JPEG/data URL/缩略图未上传 | 待补证据 |
| 云端模式上传前必须出现确认 | 前端新增云端上传确认勾选，后端 `cloud-intents` 暴露 `requiresConsent` | 云端模式未勾选不能提交、勾选后才上传的录屏/截图 | 待补证据 |
| 云端文件到期自动删除 | 云端任务写入 `expiresAt`，`cleanupExpiredJobs` 清理文件并置 `expired` | 缩短过期时间复测，确认下载链接到期后 404 | 待补证据 |
| 手动删除后链接立即失效 | DELETE 接口调用 `deleteCloudJob`，Phase 2 smoke 覆盖删除后下载 404 | 手动删除操作和原下载链接立即 404 的接口/页面证据 | 待补证据 |

---

## 2. 抓包记录模板

| 场景 | 环境 | 素材 ID | 预期 | 实际 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 本地原始素材 | 待填 | 待填 | 不出现原始素材上传请求 | 待测 | 待补 |
| 本地缩略图 | 待填 | 待填 | 不出现缩略图/封面帧上传请求 | 待测 | 待补 |
| 云端确认 | 待填 | 待填 | 未确认不能上传，确认后才 POST | 待测 | 待补 |
| 云端过期 | 待填 | 待填 | 到期后下载链接 404 | 待测 | 待补 |
| 手动删除 | 待填 | 待填 | 删除后原链接立即 404 | 待测 | 待补 |

---

## 3. 通过口径

1. 自动检查 `pnpm run privacy:check` 必须无缺失实现片段。
2. 本地模式必须有抓包或浏览器网络日志，明确没有原始素材和缩略图上传。
3. 云端模式必须有上传前确认的页面证据。
4. 云端到期删除和手动删除必须有接口状态码或页面证据。
5. 全部证据补齐后，才允许把顶部状态改为：

```text
> **隐私测试手动验收状态**：通过
```

6. 严格验收 `pnpm run privacy:check:strict` 通过后，11.3 才能算完整通过。

隐私测试别靠口头承诺。没抓包、没状态码、没证据，就当没测。
