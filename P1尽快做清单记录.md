# VidLive - P1 MVP 后尽快做清单记录

## 文档说明

- **来源章节**：`开发周期文档.md` 13. 任务优先级 / P1：MVP 后尽快做
- **执行日期**：2026年6月4日
- **自动检查入口**：`pnpm run p1:check`
- **严格验收入口**：`pnpm run p1:check:strict`

> **P1 尽快做清单手动验收状态**：待补证据

当前记录用于把 P1 能力落成可重复检查入口。工程侧已补云端队列、R2 签名链接、背景色填充、二维码和监控入口；真实 Redis、R2、手机扫码、云端转换和日志样例仍必须补实测证据。

---

## 1. P1 执行状态

| P1 项 | 自动覆盖 | 必须补齐的真实证据 | 当前状态 |
| --- | --- | --- | --- |
| 云端处理兜底 | 本地超限切云端、云端确认、上传任务和失败提示 | 本地失败或超限后切云端的完整录屏/日志 | 待补证据 |
| Fastify 上传/转换/状态 API | 上传、状态、下载、删除、metrics API | 真实上传、轮询、下载、删除和异常返回记录 | 待补证据 |
| Redis + BullMQ Worker | `bullmq`、`ioredis`、队列、Worker、独立 worker 脚本 | Redis 环境下任务入队、Worker 消费、失败重试记录 | 待补证据 |
| R2 临时链接 | S3/R2 SDK、PutObject、签名 GetObject URL、删除入口 | R2 上传、签名链接过期、手动删除失效记录 | 待补证据 |
| 旋转、翻转、背景色填充 | Web 控件、本地 Canvas、云端 FFmpeg 滤镜和 shared draft | 三类编辑导出样例截图/产物 | 待补证据 |
| 二维码发送到手机 | 云端完成后生成本地 QR data URL | 手机扫码下载成功记录 | 待补证据 |
| 基础监控和日志 | Fastify/Pino 日志、转换任务 metrics、状态统计 | 任务成功/失败日志和 metrics 截图/JSON | 待补证据 |

---

## 2. 验收模板

| P1 项 | 环境 | 素材 ID | 操作 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 云端处理兜底 | 待填 | 待填 | 待填 | 待测 | 待补 |
| Fastify 上传/转换/状态 API | 待填 | 待填 | 待填 | 待测 | 待补 |
| Redis + BullMQ Worker | 待填 | 待填 | 待填 | 待测 | 待补 |
| R2 临时链接 | 待填 | 待填 | 待填 | 待测 | 待补 |
| 旋转、翻转、背景色填充 | 待填 | 待填 | 待填 | 待测 | 待补 |
| 二维码发送到手机 | 待填 | 待填 | 待填 | 待测 | 待补 |
| 基础监控和日志 | 待填 | 待填 | 待填 | 待测 | 待补 |

---

## 3. 通过口径

1. 自动检查 `pnpm run p1:check` 必须无缺失实现片段。
2. Redis + BullMQ 必须用真实 Redis 跑一次入队、消费、完成、失败重试。
3. R2 必须验证签名链接可用、过期不可用、手动删除立即失效。
4. 二维码必须用手机扫码完成下载，不允许只看前端有图。
5. 旋转、翻转、背景色填充必须有导出产物或截图。
6. 基础监控必须记录成功、失败和当前队列状态。
7. 全部证据补齐后，才允许把顶部状态改为：

```text
> **P1 尽快做清单手动验收状态**：通过
```

8. 严格验收 `pnpm run p1:check:strict` 通过后，P1 才能算完整完成。

P1 不是“后面再说”的垃圾桶。这里补的是 MVP 之后最容易影响体验和稳定性的硬能力。
