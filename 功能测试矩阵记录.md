# VidLive - 功能测试矩阵记录

## 文档说明

- **来源章节**：`开发周期文档.md` 11.1 功能测试矩阵
- **执行日期**：2026年6月4日
- **自动检查入口**：`pnpm run function-matrix:check`
- **严格验收入口**：`pnpm run function-matrix:check:strict`

> **功能测试矩阵手动验收状态**：待补证据

当前记录用于把 11.1 的测试矩阵落成可重复检查入口。自动检查只能证明代码里存在支持、限制、预设和错误分流；真实通过必须补齐样例素材、浏览器环境、云端任务和真机记录。

---

## 1. 矩阵执行状态

| 类型 | 测试项 | 自动覆盖 | 必须补齐的真实证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| 输入格式 | MP4、MOV、GIF、异常扩展名、损坏文件 | `supportedInputs`、上传格式拦截、`unsupported-format` 提示 | 3 个正常素材、1 个伪装扩展名、1 个损坏文件的导入/拒绝记录 | 待补证据 |
| 文件大小 | 10MB、50MB、100MB、超过 100MB | 本地 100MB 限制、云端 500MB 限制、413 错误 | 10MB、50MB、100MB 本地流程，以及超过 100MB 切云端流程 | 待补证据 |
| 时长 | 1 秒、2 秒、3 秒、10 秒、30 秒、超过 30 秒 | 最短 1 秒、推荐最长 30 秒、裁剪 clamp | 各时长样例导入、裁剪、导出记录；超过 30 秒必须触发裁剪/提示 | 待补证据 |
| 比例 | 9:16、1:1、4:5、16:9、横屏转竖屏 | 比例预设、Canvas 尺寸、旋转/裁切链路 | 四个比例导出截图/产物；横屏素材转 9:16 结果 | 待补证据 |
| 编码 | H.264、HEVC、可变帧率、无音频、有音频 | `ffprobe` 探测、H.264 MOV 输出、音频可选映射 | H.264、HEVC、VFR、有音频、无音频样例的处理结果 | 待补证据 |
| 导出 | 标准 Live Photo、锁屏预设、MP4、GIF、ZIP | 预设定义、ZIP 打包、本地/云端导出链路 | 标准 Live Photo、锁屏预设、MP4、GIF、ZIP 产物下载和打开记录 | 待补证据 |
| 错误 | 文件过大、编码不支持、浏览器内存不足、云端超时 | `failureAdvice`、413、415、轮询超时提示 | 文件过大、编码不支持、浏览器内存不足、云端超时的复现实录 | 待补证据 |

---

## 2. 样例素材要求

| 样例 ID | 用途 | 要求 |
| --- | --- | --- |
| FM-IN-MP4-H264 | 输入格式、编码、导出 | MP4、H.264、含音频，约 3 秒 |
| FM-IN-MOV-H264 | 输入格式、导出 | MOV、H.264，约 3 秒 |
| FM-IN-GIF | 输入格式、GIF 导出 | GIF 动图，约 2-3 秒 |
| FM-IN-BAD-EXT | 异常扩展名 | 视频内容伪装成 `.txt` 或不支持扩展名 |
| FM-IN-CORRUPT | 损坏文件 | 截断或破坏容器头的 MP4/MOV |
| FM-SIZE-10 | 文件大小 | 约 10MB，能完成本地导出 |
| FM-SIZE-50 | 文件大小 | 约 50MB，能完成本地导出 |
| FM-SIZE-100 | 文件大小 | 接近 100MB，验证本地上限边界 |
| FM-SIZE-OVER-100 | 文件大小 | 超过 100MB，验证云端兜底 |
| FM-DUR-1 | 时长 | 1 秒 |
| FM-DUR-2 | 时长 | 2 秒 |
| FM-DUR-3 | 时长 | 3 秒 |
| FM-DUR-10 | 时长 | 10 秒 |
| FM-DUR-30 | 时长 | 30 秒 |
| FM-DUR-OVER-30 | 时长 | 超过 30 秒，必须裁剪或提示 |
| FM-RATIO-LANDSCAPE | 比例 | 横屏素材，用于转 9:16 |
| FM-CODEC-HEVC | 编码 | HEVC 输入，验证本地失败或云端兜底 |
| FM-CODEC-VFR | 编码 | 可变帧率输入 |
| FM-CODEC-NO-AUDIO | 编码 | 无音频输入 |
| FM-CODEC-AUDIO | 编码 | 有音频输入 |

---

## 3. 通过口径

1. 自动检查 `pnpm run function-matrix:check` 必须无缺失实现片段。
2. 每个测试项必须有素材 ID、环境、操作步骤、结果和失败原因记录。
3. 输入、文件大小、时长、比例、编码、导出、错误 7 类均完成后，才允许把顶部状态改为：

```text
> **功能测试矩阵手动验收状态**：通过
```

4. 严格验收 `pnpm run function-matrix:check:strict` 通过后，11.1 才能算完整通过。

未补真实证据时，不要把状态改成通过。矩阵不是许愿池，填“通过”但没有素材和环境，后面一测就穿帮。

---

## 4. 2026-06-04 局部复测记录

| 类型 | 环境 | 素材 ID | 操作 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 输入格式 | Chrome Headless 148.0.7778.217 | `dod-valid.gif` | 选择素材 -> 读取文件 | 通过；文件名可见，生成按钮可用 | `tmp/dod-cdp-report.json` |
| 输入格式 | Chrome Headless 148.0.7778.217 | `dod-valid.mp4` | 选择素材 -> 读取元信息 | 通过；文件名可见，生成按钮可用 | `tmp/dod-cdp-report.json` |
| 导出 | Chrome Headless 148.0.7778.217 | `dod-valid.gif` | 本地生成 | 通过；出现“导出结果”和“下载 ZIP 包” | `tmp/dod-cdp-report.json` |
| 导出 | Chrome Headless 148.0.7778.217 | `dod-valid.mp4` | 本地生成 | 阻塞/不稳定；稳定退出报告出现“本地转换失败”，此前同环境曾生成 ZIP，需真实浏览器复测 | `tmp/dod-cdp-report.json` |
| 错误 | Chrome Headless 148.0.7778.217 | `dod-invalid.txt` | 选择素材 | 通过；出现“格式不支持” | `tmp/dod-cdp-report.json` |

以上只补了局部自动证据，尚未覆盖 MOV、文件大小边界、10/30 秒样例、HEVC/VFR、有音频、浏览器内存不足、云端超时等矩阵项，所以顶部状态继续保持“待补证据”。
