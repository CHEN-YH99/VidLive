# VidLive - 兼容测试矩阵记录

## 文档说明

- **来源章节**：`开发周期文档.md` 11.2 兼容测试矩阵
- **执行日期**：2026年6月4日
- **自动检查入口**：`pnpm run compatibility-matrix:check`
- **严格验收入口**：`pnpm run compatibility-matrix:check:strict`

> **兼容测试矩阵手动验收状态**：待补证据

当前记录用于把 11.2 的平台兼容矩阵落成可重复检查入口。自动检查只能确认代码和文档里有对应平台、保存路径、导出链路和反馈入口；iPhone Safari、macOS Safari、Edge、iOS 17+ 真机这些平台仍必须真实跑一遍。

---

## 1. 矩阵执行状态

| 平台 | 必测项 | 自动覆盖 | 必须补齐的真实证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| iPhone Safari | 导入、裁剪、导出、保存指引、锁屏设置 | 上传/裁剪/导出 UI、iPhone 保存路径、锁屏路径、兼容反馈入口 | iPhone Safari 全流程录屏、导出包、保存路径、锁屏设置结果 | 待补证据 |
| macOS Safari | 导入、导出、下载 ZIP、AirDrop 路径 | 桌面下载路径、ZIP 导出、AirDrop 指引 | macOS Safari 导入导出、ZIP 下载、AirDrop 到 iPhone 的结果 | 待补证据 |
| Chrome 桌面 | 本地处理、MP4/GIF 导出 | 本地导出函数、MediaRecorder、MP4/GIF 兜底预设 | Chrome 桌面本地处理、MP4/GIF 产物打开记录 | Headless 局部通过，手动下载打开待补 |
| Edge 桌面 | 本地处理、下载行为 | 本地导出函数、`downloadBlob`、桌面下载路径 | Edge 桌面本地处理、下载行为截图/录屏 | 待补证据 |
| iOS 17+ 真机 | 标准 Live Photo 识别、锁屏播放 | Phase 0 POC、Live Photo 元数据、iOS 17+ 锁屏校验说明 | 至少 3 台 iPhone、覆盖 iOS 17+ 两个主要版本、相册识别和锁屏播放结果 | 待补证据 |

---

## 2. 平台验收模板

| 平台 | 设备/系统版本 | 浏览器版本 | 素材 ID | 测试步骤 | 结果 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| iPhone Safari | 待填 | 待填 | 待填 | 导入 -> 裁剪 -> 导出 -> 保存 -> 锁屏设置 | 待测 | 待补 |
| macOS Safari | 待填 | 待填 | 待填 | 导入 -> 导出 -> 下载 ZIP -> AirDrop | 待测 | 待补 |
| Chrome 桌面 | 待填 | 待填 | 待填 | 本地处理 -> 下载 MP4/GIF | 待测 | 待补 |
| Chrome Headless 桌面 | Windows 本机 | 148.0.7778.217 | `dod-valid.gif`、`dod-valid.mp4` | 导入 -> 本地生成 -> 出现导出结果 | 部分通过；GIF 通过，MP4 在稳定退出报告中出现“本地转换失败”，需真实浏览器复测 | `tmp/dod-cdp-report.json`、`tmp/dod-screenshots/desktop-1440x1000.png` |
| Chrome Headless 移动视口 | Windows 本机 / 390x844 | 148.0.7778.217 | 无素材 | 首页渲染 -> 检查横向溢出 | 通过；非空白，`scrollWidth=390`，无横向溢出。不是 iPhone Safari 真机 | `tmp/dod-cdp-report.json`、`tmp/dod-screenshots/mobile-390x844.png` |
| Edge 桌面 | 待填 | 待填 | 待填 | 本地处理 -> 下载产物 | 待测 | 待补 |
| iOS 17+ 真机 | 待填 | 相册/锁屏 | 待填 | 导入 Live Photo 产物 -> 相册识别 -> 锁屏播放 | 待测 | 待补 |

---

## 3. 通过口径

1. 自动检查 `pnpm run compatibility-matrix:check` 必须无缺失实现片段。
2. 5 个平台都必须有设备/系统版本、浏览器版本、素材 ID、结果和证据。
3. iOS 17+ 真机必须至少覆盖 3 台 iPhone，并至少覆盖 iOS 17+ 两个主要版本。
4. 全部平台证据补齐后，才允许把顶部状态改为：

```text
> **兼容测试矩阵手动验收状态**：通过
```

5. 严格验收 `pnpm run compatibility-matrix:check:strict` 通过后，11.2 才能算完整通过。

没有真机、没有浏览器版本、没有录屏或截图，就不要写通过。兼容测试不是写诗，不能靠想象押韵。
