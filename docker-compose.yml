services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: vidlive
      POSTGRES_USER: vidlive
      POSTGRES_PASSWORD: vidlive
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
