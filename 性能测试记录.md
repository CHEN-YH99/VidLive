# VidLive - 性能测试记录

## 文档说明

- **来源章节**：`开发周期文档.md` 11.4 性能测试
- **执行日期**：2026年6月4日
- **自动检查入口**：`pnpm run performance:check`
- **严格验收入口**：`pnpm run performance:check:strict`

> **性能测试手动验收状态**：待补证据

当前记录用于把 11.4 的性能指标落成可重复检查入口。自动检查只确认产品里存在对应的本地解析、本地转换、云端任务、ZIP 打包和首屏入口；P75 必须由真实样本统计得出。

---

## 1. 性能指标状态

| 指标 | 目标 | 自动覆盖 | 必须补齐的真实证据 | 当前状态 |
| --- | --- | --- | --- | --- |
| 首屏可交互 | 小于 3 秒 | 首页直接渲染工具页，Next 生产构建可用 | Lighthouse/WebPageTest 或浏览器 Performance 记录，至少 5 次样本 P75 | 待补证据 |
| 本地解析 100MB 内视频 | P75 小于 10 秒 | 100MB 本地上限、`metadata` 预加载、对象 URL 读取 | 10MB/50MB/100MB 样本解析耗时，至少 5 次样本 P75 | 待补证据 |
| 本地转换 10 秒 1080p 视频 | P75 小于 60 秒 | 10 秒本地目标、MediaRecorder、`performance.now()` 超时保护 | 10 秒 1080p 本地导出耗时，至少 5 次样本 P75 | 待补证据 |
| 云端转换 30 秒 1080p 视频 | P75 小于 30 秒 | 云端任务提交、轮询、FFmpeg 转换、Phase 2 smoke 链路 | 30 秒 1080p 云端任务耗时，至少 5 次样本 P75 | 待补证据 |
| 导出包生成 | P75 小于 10 秒 | 本地 ZIP、云端 ZIP、产物大小检查 | ZIP 生成耗时，至少 5 次样本 P75 | 待补证据 |

---

## 2. P75 采样模板

| 指标 | 环境 | 素材 ID | 样本耗时 ms | P75 ms | 是否达标 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| 首屏可交互 | 待填 | 不适用 | 待填 | 待填 | 待测 | 待补 |
| 本地解析 100MB 内视频 | 待填 | 待填 | 待填 | 待填 | 待测 | 待补 |
| 本地转换 10 秒 1080p 视频 | 待填 | 待填 | 待填 | 待填 | 待测 | 待补 |
| 云端转换 30 秒 1080p 视频 | 待填 | 待填 | 待填 | 待填 | 待测 | 待补 |
| 导出包生成 | 待填 | 待填 | 待填 | 待填 | 待测 | 待补 |

---

## 3. 通过口径

1. 自动检查 `pnpm run performance:check` 必须无缺失实现片段。
2. 每个指标至少 5 次样本，记录原始耗时和 P75。
3. 本地解析必须覆盖接近 100MB 的素材。
4. 本地转换必须覆盖 10 秒 1080p 视频。
5. 云端转换必须覆盖 30 秒 1080p 视频。
6. 全部指标达标后，才允许把顶部状态改为：

```text
> **性能测试手动验收状态**：通过
```

7. 严格验收 `pnpm run performance:check:strict` 通过后，11.4 才能算完整通过。

P75 不是感觉。没有样本、没有耗时、没有环境，就别写达标。
