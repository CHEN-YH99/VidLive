# VidLive - 技术实现文档

## 文档说明
- **版本**：v1.1
- **更新日期**：2026年6月4日
- **目标**：Phase 0 后同步的本地优先 MVP 技术实现方案

---

## Phase 0 后同步调整摘要

Phase 0 之后，本文档的 MVP 口径统一调整为“浏览器本地优先，云端作为 P1/Beta 兜底”。旧版文档里把数据库、Redis、R2、AI 关键帧、账号和支付一股脑塞进 MVP，范围偏大，像赶集一样热闹但不好落地。当前以现有工程为准：

1. MVP 默认在浏览器本地导入、裁剪、选关键帧和生成导出包，小文件不上传。
2. 云端处理属于 P1/Beta 兜底能力：本地超限、失败或用户主动确认后再上传。
3. AI 关键帧从 MVP 移出，MVP 只保留手动关键帧和轻量候选提示。
4. 用户系统、数据库持久化、配额历史、支付订阅全部后置，不作为 MVP 完成条件。
5. Live Photo 可用性必须以 iPhone 保存路径和真机兼容矩阵为准，不能只看 ZIP 是否生成。
6. Next.js、React、Fastify、pnpm 等版本以当前仓库 `package.json` 和构建结果统一。

---

## 整体架构设计

### 系统架构图
```
┌─────────────┐
│   浏览器    │
│  Next.js UI │
└──────┬──────┘
       │ 本地默认：File API / video / canvas / MediaRecorder / ZIP
       ▼
┌─────────────┐
│ 本地导出包  │  MP4/GIF/WebP/manifest/README/ZIP
└─────────────┘

仅在 P1/Beta 兜底场景：

┌─────────────┐      HTTPS + 用户确认      ┌──────────────┐
│   浏览器    │ ───────────────────────► │ Fastify API  │
└─────────────┘                           └──────┬───────┘
                                                  │
                              ┌───────────────────┼───────────────────┐
                              │                   │                   │
                              ▼                   ▼                   ▼
                      本地临时文件链接       Redis/BullMQ(P1+)     R2签名链接(P1+)
                      local-temp-link        队列/Worker            对象存储兜底
```

MVP 不依赖账号、数据库、Redis、R2 或支付。Fastify 云端任务 API 已作为 Beta 兜底存在，但不应被写成 MVP 主路径；用户必须先看到上传、保存时长和删除规则。

### 技术栈选型

| 层级 | 技术选型 | 当前口径 |
| --- | --- | --- |
| 包管理 | pnpm | 10.33.0 |
| 运行时 | Node.js | `>=20.0.0`，本机验证 Node 24.13.1 |
| 前端框架 | Next.js App Router | package range `^15.0.0`，构建验证 15.5.19 |
| 前端运行库 | React / React DOM | `^19.0.0` |
| 样式 | Tailwind CSS | `^3.4.0` |
| 前端本地处理 | File API、`<video>`、`<canvas>`、MediaRecorder、Blob ZIP | MVP 主路径 |
| 后端框架 | Fastify | `^5.0.0`，P1/Beta 云端兜底 |
| 视频处理 | FFmpeg / ffprobe | Phase 0/P1 云端与 POC 需要；本机验证 FFmpeg 8.1 可用 |
| 元数据处理 | exiftool | Phase 0 真机验收需要；当前本机缺失，Dockerfile 有 `perl-image-exiftool` 兜底 |
| 数据库 | PostgreSQL | 后置到用户/历史/配额阶段，不是 MVP 依赖 |
| 缓存/队列 | Redis + BullMQ | P1+ 云端队列增强，不是 MVP 依赖 |
| 对象存储 | Cloudflare R2 | P1+ 签名链接增强；MVP/P1 Beta 可使用 `local-temp-link` |


---

## 后置数据库设计

MVP 不创建用户表、转换记录表或支付表。下面的表结构保留为 P2/P3 之后的用户系统、历史记录、配额和商业化参考；不要把它写进 MVP 必交付，阿叔看了都觉得你在给自己加班。

### 表结构设计（P2/P3+）

#### 1. users 表（用户表）
```sql
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    username VARCHAR(50) UNIQUE NOT NULL,
    plan_type VARCHAR(20) DEFAULT 'free', -- free, pro
    daily_quota INT DEFAULT 5, -- 免费用户每日配额
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_users_email ON users(email);
```

#### 2. conversions 表（转换记录）
```sql
CREATE TABLE conversions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    original_filename VARCHAR(255) NOT NULL,
    original_size BIGINT NOT NULL, -- 字节
    video_duration FLOAT NOT NULL, -- 秒
    status VARCHAR(20) NOT NULL, -- pending, processing, completed, failed
    keyframe_time FLOAT, -- 选择的关键帧时间点
    output_url TEXT, -- 生成文件的下载URL
    error_message TEXT,
    processing_time INT, -- 处理耗时（毫秒）
    created_at TIMESTAMP DEFAULT NOW(),
    expires_at TIMESTAMP -- 文件过期时间
);

CREATE INDEX idx_conversions_user ON conversions(user_id);
CREATE INDEX idx_conversions_status ON conversions(status);
CREATE INDEX idx_conversions_expires ON conversions(expires_at);
```

#### 3. usage_logs 表（用量统计）
```sql
CREATE TABLE usage_logs (
    id BIGSERIAL PRIMARY KEY,
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    action VARCHAR(50) NOT NULL, -- upload, convert, download
    timestamp TIMESTAMP DEFAULT NOW(),
    metadata JSONB -- 额外信息
);

CREATE INDEX idx_usage_user_time ON usage_logs(user_id, timestamp);
```


---

## 核心功能技术实现

### 1. 素材导入与本地处理模块

#### MVP 实现口径

MVP 首屏就是工具页，不走登录，不默认上传。小文件路径如下：

1. `react-dropzone` 接收 MP4/MOV/GIF，默认上限 100MB。
2. `URL.createObjectURL(file)` 生成本地预览地址。
3. `<video>` 读取时长、宽高；GIF 走图片类元信息兜底。
4. `<canvas>` 抓封面帧，MediaRecorder 录制裁剪后的动态片段。
5. 浏览器内打包 ZIP，包含动态片段、封面、manifest 和保存指引。
6. 本地模式不得出现 `fetch`、`XMLHttpRequest`、`axios` 或 `FormData` 上传原始素材。

云端只在 P1/Beta 兜底时启用：超过本地上限、本地转码失败、或用户主动切换云端并勾选上传确认。这个边界一定要写死，不然隐私卖点直接塌房。

#### 前端实现（Next.js）
当前工程入口是 `apps/web/components/vidlive-tool.tsx`，本地导出核心在 `apps/web/lib/local-export.ts`。下面旧版上传示例只作为 P1 云端模式参考，不再代表 MVP 主路径。

```typescript
// app/upload/page.tsx
'use client';

import { useState } from 'react';
import { useDropzone } from 'react-dropzone';

export default function UploadPage() {
  const [uploadProgress, setUploadProgress] = useState(0);
  const [videoUrl, setVideoUrl] = useState<string | null>(null);

  const { getRootProps, getInputProps } = useDropzone({
    accept: { 'video/*': ['.mp4', '.mov', '.avi', '.mkv'] },
    maxSize: 500 * 1024 * 1024, // 500MB
    onDrop: async (files) => {
      const file = files[0];
      await uploadVideo(file);
    },
  });

  const uploadVideo = async (file: File) => {
    const formData = new FormData();
    formData.append('video', file);

    const xhr = new XMLHttpRequest();
    xhr.upload.addEventListener('progress', (e) => {
      setUploadProgress(Math.round((e.loaded / e.total) * 100));
    });

    xhr.open('POST', '/api/upload');
    xhr.send(formData);

    xhr.onload = () => {
      const result = JSON.parse(xhr.responseText);
      setVideoUrl(result.videoUrl);
    };
  };

  return (
    <div {...getRootProps()} className="border-2 border-dashed p-8">
      <input {...getInputProps()} />
      <p>拖拽视频到这里，或点击选择文件</p>
      {uploadProgress > 0 && <p>上传进度: {uploadProgress}%</p>}
    </div>
  );
}
```

#### 后端实现（Fastify，P1/Beta 兜底）
Fastify 上传和任务接口属于云端兜底，不是 MVP 默认路径。当前工程使用 `/api/conversions/cloud-jobs` 创建云端任务，完成后返回本地临时链接或 R2 签名链接。

```typescript
// src/routes/upload.ts
import { FastifyInstance } from 'fastify';
import multipart from '@fastify/multipart';
import { v4 as uuidv4 } from 'uuid';
import fs from 'fs/promises';
import path from 'path';

export async function uploadRoutes(fastify: FastifyInstance) {
  fastify.register(multipart, {
    limits: { fileSize: 500 * 1024 * 1024 },
  });

  fastify.post('/upload', async (request, reply) => {
    const data = await request.file();
    if (!data) throw new Error('No file uploaded');

    // 生成唯一文件名
    const fileId = uuidv4();
    const ext = path.extname(data.filename);
    const filename = `${fileId}${ext}`;
    const uploadPath = path.join(process.env.UPLOAD_DIR!, filename);

    // 保存文件
    await fs.writeFile(uploadPath, await data.toBuffer());

    // 获取视频元信息
    const metadata = await getVideoMetadata(uploadPath);

    // 入库
    const conversion = await fastify.db.conversions.create({
      userId: request.user.id,
      originalFilename: data.filename,
      originalSize: metadata.size,
      videoDuration: metadata.duration,
      status: 'pending',
    });

    return {
      conversionId: conversion.id,
      videoUrl: `/uploads/${filename}`,
      duration: metadata.duration,
    };
  });
}
```


### 2. 关键帧选择与 AI 推荐（AI 后置）

MVP 不接入 OpenCV、模型推理或服务端 AI 推荐。当前 MVP 只需要：

- 手动拖动关键帧时间点。
- 根据视频时长给出中点、前段、后段这类轻量候选提示。
- 导出 manifest 记录关键帧时间，方便真机复测。

下面的 OpenCV/清晰度/人脸检测方案后置到 Phase 3/V1 或更晚，只有在真实用户证明“自动选帧”有价值后再投入。别一上来就堆 AI，视频还没稳定保存到 iPhone，相当于饭没煮熟先雕萝卜花。

#### 算法实现（后置版本）
```typescript
// src/services/keyframe-detector.ts
import { exec } from 'child_process';
import { promisify } from 'util';
import cv from '@u4/opencv4nodejs'; // OpenCV Node绑定

const execAsync = promisify(exec);

export class KeyframeDetector {
  /**
   * 提取视频的所有I帧（关键帧）
   */
  async extractIFrames(videoPath: string, outputDir: string): Promise<string[]> {
    const cmd = `ffmpeg -i "${videoPath}" -vf "select='eq(pict_type,I)'" -vsync 0 "${outputDir}/frame_%04d.jpg"`;
    await execAsync(cmd);

    // 返回生成的图片路径列表
    const files = await fs.readdir(outputDir);
    return files.map(f => path.join(outputDir, f));
  }

  /**
   * 计算图片清晰度（Laplacian方差）
   */
  calculateSharpness(imagePath: string): number {
    const img = cv.imread(imagePath);
    const gray = img.cvtColor(cv.COLOR_BGR2GRAY);
    const laplacian = gray.laplacian(cv.CV_64F);
    const { stddev } = laplacian.meanStdDev();
    return stddev.w * stddev.w; // 方差越大越清晰
  }

  /**
   * 人脸检测（可选增强）
   */
  async detectFaces(imagePath: string): Promise<number> {
    const img = cv.imread(imagePath);
    const classifier = new cv.CascadeClassifier(cv.HAAR_FRONTALFACE_ALT2);
    const { objects } = classifier.detectMultiScale(img.bgrToGray());
    return objects.length;
  }

  /**
   * 智能推荐最佳关键帧
   */
  async recommendKeyframes(videoPath: string, topN: number = 3): Promise<RecommendedFrame[]> {
    const tempDir = `/tmp/${uuidv4()}`;
    await fs.mkdir(tempDir, { recursive: true });

    // 1. 提取所有I帧
    const frames = await this.extractIFrames(videoPath, tempDir);

    // 2. 评分计算
    const scored = await Promise.all(
      frames.map(async (framePath, idx) => {
        const sharpness = this.calculateSharpness(framePath);
        const faceCount = await this.detectFaces(framePath);

        // 综合评分：清晰度 + 人脸加权
        const score = sharpness + faceCount * 1000;

        return {
          framePath,
          frameIndex: idx,
          timestamp: this.getTimestamp(videoPath, idx),
          score,
          sharpness,
          faceCount,
        };
      })
    );

    // 3. 排序并返回Top N
    return scored.sort((a, b) => b.score - a.score).slice(0, topN);
  }

  /**
   * 根据帧索引计算时间戳
   */
  private async getTimestamp(videoPath: string, frameIndex: number): Promise<number> {
    const { stdout } = await execAsync(
      `ffprobe -v error -select_streams v:0 -show_entries stream=r_frame_rate -of csv=p=0 "${videoPath}"`
    );
    const [num, den] = stdout.trim().split('/').map(Number);
    const fps = num / den;
    return frameIndex / fps;
  }
}
```

#### API接口
```typescript
// src/routes/analyze.ts
fastify.post('/analyze/:conversionId', async (request, reply) => {
  const { conversionId } = request.params;
  const conversion = await fastify.db.conversions.findById(conversionId);

  const detector = new KeyframeDetector();
  const recommendations = await detector.recommendKeyframes(
    conversion.videoPath,
    3
  );

  return { recommendations };
});
```


### 3. Live Photo 生成核心

#### 格式规范说明
Live Photo由两个文件组成：
- **静态图片**：HEIC格式（或JPEG兼容）
- **视频片段**：MOV格式，H.264编码，AAC音频

**关键元数据（必须）**：
```
com.apple.quicktime.content.identifier: 唯一UUID
com.apple.quicktime.live-photo.auto: 1
com.apple.quicktime.make: Apple
```

#### 生成器实现
```typescript
// src/services/livephoto-generator.ts
import { exec } from 'child_process';
import { promisify } from 'util';
import { v4 as uuidv4 } from 'uuid';
import archiver from 'archiver';

const execAsync = promisify(exec);

export class LivePhotoGenerator {
  /**
   * 生成Live Photo
   * @param videoPath 原始视频路径
   * @param keyframeTime 关键帧时间点（秒）
   * @param duration Live Photo视频时长（秒，默认3秒）
   */
  async generate(
    videoPath: string,
    keyframeTime: number,
    duration: number = 3
  ): Promise<{ photoPath: string; videoPath: string; zipPath: string }> {
    const workDir = `/tmp/livephoto_${uuidv4()}`;
    await fs.mkdir(workDir, { recursive: true });

    // 1. 提取关键帧为静态图片
    const photoPath = path.join(workDir, 'photo.jpg');
    await execAsync(
      `ffmpeg -i "${videoPath}" -ss ${keyframeTime} -frames:v 1 -q:v 2 "${photoPath}"`
    );

    // 2. 转换为HEIC格式（可选，需要libheif支持）
    const heicPath = path.join(workDir, 'photo.heic');
    try {
      await execAsync(`heif-enc -q 90 "${photoPath}" -o "${heicPath}"`);
    } catch {
      // 降级：如果HEIC转换失败，使用JPEG
      console.warn('HEIC conversion failed, fallback to JPEG');
      await fs.copyFile(photoPath, heicPath.replace('.heic', '.jpg'));
    }

    // 3. 裁剪视频片段（关键帧前后各1.5秒）
    const videoStart = Math.max(0, keyframeTime - 1.5);
    const movPath = path.join(workDir, 'video.mov');
    await execAsync(
      `ffmpeg -i "${videoPath}" -ss ${videoStart} -t ${duration} \
       -c:v libx264 -preset fast -crf 23 \
       -c:a aac -b:a 128k -ar 44100 \
       -movflags +faststart \
       "${movPath}"`
    );

    // 4. 注入Live Photo元数据（关键步骤！）
    const contentId = uuidv4().toUpperCase();
    await this.injectMetadata(movPath, heicPath, contentId);

    // 5. 打包为ZIP
    const zipPath = path.join(workDir, 'livephoto.zip');
    await this.createZip(heicPath, movPath, zipPath);

    return {
      photoPath: heicPath,
      videoPath: movPath,
      zipPath,
    };
  }

  /**
   * 注入Live Photo元数据
   */
  private async injectMetadata(
    movPath: string,
    heicPath: string,
    contentId: string
  ): Promise<void> {
    // MOV文件元数据
    const movMetadata = [
      `-QuickTime:ContentIdentifier="${contentId}"`,
      `-QuickTime:LivePhotoAuto=1`,
      `-QuickTime:Make=Apple`,
      `-QuickTime:Model="iPhone 14 Pro"`, // 伪装设备
    ].join(' ');

    await execAsync(`exiftool -overwrite_original ${movMetadata} "${movPath}"`);

    // HEIC文件元数据（保持一致）
    const heicMetadata = [
      `-XMP:LivePhotoVideoIndex=0`,
      `-XMP:ContentIdentifier="${contentId}"`,
    ].join(' ');

    await execAsync(`exiftool -overwrite_original ${heicMetadata} "${heicPath}"`);
  }

  /**
   * 打包为ZIP文件
   */
  private async createZip(
    photoPath: string,
    videoPath: string,
    zipPath: string
  ): Promise<void> {
    return new Promise((resolve, reject) => {
      const output = fs.createWriteStream(zipPath);
      const archive = archiver('zip', { zlib: { level: 9 } });

      output.on('close', resolve);
      archive.on('error', reject);

      archive.pipe(output);
      archive.file(photoPath, { name: path.basename(photoPath) });
      archive.file(videoPath, { name: path.basename(videoPath) });
      archive.finalize();
    });
  }
}
```


### 4. 异步任务队列（P1+）

MVP 不需要 Redis/BullMQ。P1/Beta 阶段可以先用进程内任务和 `local-temp-link` 跑通上传、轮询、下载、删除；当真实云端转换量上来，再接 Redis/BullMQ 和 R2。不要为了“架构完整”把 MVP 搞成运维练习题。

#### BullMQ队列配置
```typescript
// src/queues/conversion-queue.ts
import { Queue, Worker, Job } from 'bullmq';
import { Redis } from 'ioredis';

const connection = new Redis({
  host: process.env.REDIS_HOST,
  port: parseInt(process.env.REDIS_PORT!),
  maxRetriesPerRequest: null,
});

// 创建任务队列
export const conversionQueue = new Queue('video-conversion', {
  connection,
  defaultJobOptions: {
    attempts: 3, // 失败重试3次
    backoff: { type: 'exponential', delay: 5000 },
    removeOnComplete: { count: 100 }, // 保留最近100条成功记录
    removeOnFail: { count: 500 },
  },
});

// Worker处理器
const worker = new Worker(
  'video-conversion',
  async (job: Job) => {
    const { conversionId, videoPath, keyframeTime, duration } = job.data;

    // 更新状态为处理中
    await db.conversions.update(conversionId, { status: 'processing' });

    try {
      // 生成Live Photo
      const generator = new LivePhotoGenerator();
      const result = await generator.generate(videoPath, keyframeTime, duration);

      // 上传到对象存储
      const downloadUrl = await uploadToR2(result.zipPath);

      // 更新数据库
      await db.conversions.update(conversionId, {
        status: 'completed',
        outputUrl: downloadUrl,
        processingTime: Date.now() - job.timestamp,
        expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000), // 7天后过期
      });

      // 清理临时文件
      await fs.rm(path.dirname(result.zipPath), { recursive: true });

      return { success: true, downloadUrl };
    } catch (error) {
      await db.conversions.update(conversionId, {
        status: 'failed',
        errorMessage: error.message,
      });
      throw error;
    }
  },
  { connection, concurrency: 5 } // 最多5个并发任务
);

worker.on('completed', (job) => {
  console.log(`Job ${job.id} completed`);
});

worker.on('failed', (job, err) => {
  console.error(`Job ${job?.id} failed:`, err);
});
```

#### API接口
```typescript
// src/routes/convert.ts
fastify.post('/convert/:conversionId', async (request, reply) => {
  const { conversionId } = request.params;
  const { keyframeTime, duration = 3 } = request.body;

  const conversion = await fastify.db.conversions.findById(conversionId);
  if (!conversion) throw new Error('Conversion not found');

  // 添加到任务队列
  const job = await conversionQueue.add('convert', {
    conversionId,
    videoPath: conversion.videoPath,
    keyframeTime,
    duration,
  });

  return {
    jobId: job.id,
    message: '任务已加入队列，预计处理时间15秒',
  };
});

// 查询任务进度
fastify.get('/convert/:conversionId/status', async (request, reply) => {
  const { conversionId } = request.params;
  const conversion = await fastify.db.conversions.findById(conversionId);

  return {
    status: conversion.status,
    outputUrl: conversion.outputUrl,
    errorMessage: conversion.errorMessage,
  };
});
```


### 5. 对象存储集成（P1+ Cloudflare R2）

R2 只作为 P1+ 云端兜底增强。Beta 阶段可以使用本地临时下载链接 `local-temp-link` 验证上传、轮询、下载和删除；只有当云端处理频率、成本和隐私文案都稳定后，再切 R2 签名链接。原始素材不应公开分发。

#### R2 SDK封装
```typescript
// src/services/storage.ts
import { S3Client, PutObjectCommand, GetObjectCommand } from '@aws-sdk/client-s3';
import { getSignedUrl } from '@aws-sdk/s3-request-presigner';
import fs from 'fs/promises';

export class StorageService {
  private s3Client: S3Client;
  private bucketName: string;

  constructor() {
    this.s3Client = new S3Client({
      region: 'auto',
      endpoint: process.env.R2_ENDPOINT, // https://xxx.r2.cloudflarestorage.com
      credentials: {
        accessKeyId: process.env.R2_ACCESS_KEY_ID!,
        secretAccessKey: process.env.R2_SECRET_ACCESS_KEY!,
      },
    });
    this.bucketName = process.env.R2_BUCKET_NAME!;
  }

  /**
   * 上传文件到R2
   */
  async upload(localPath: string, key: string): Promise<string> {
    const fileContent = await fs.readFile(localPath);

    await this.s3Client.send(
      new PutObjectCommand({
        Bucket: this.bucketName,
        Key: key,
        Body: fileContent,
        ContentType: this.getContentType(localPath),
      })
    );

    // 返回公开访问URL
    return `${process.env.R2_PUBLIC_URL}/${key}`;
  }

  /**
   * 生成临时下载链接（7天有效）
   */
  async getSignedDownloadUrl(key: string): Promise<string> {
    const command = new GetObjectCommand({
      Bucket: this.bucketName,
      Key: key,
    });

    return getSignedUrl(this.s3Client, command, { expiresIn: 7 * 24 * 3600 });
  }

  private getContentType(filePath: string): string {
    const ext = path.extname(filePath).toLowerCase();
    const types: Record<string, string> = {
      '.zip': 'application/zip',
      '.mov': 'video/quicktime',
      '.heic': 'image/heic',
      '.jpg': 'image/jpeg',
    };
    return types[ext] || 'application/octet-stream';
  }
}

// 使用示例
export async function uploadToR2(localPath: string): Promise<string> {
  const storage = new StorageService();
  const key = `livefotos/${uuidv4()}/${path.basename(localPath)}`;
  return storage.upload(localPath, key);
}
```


---

## 前端实现细节

当前工程前端以 `apps/web/components/vidlive-tool.tsx` 为准，使用原生 range/input、按钮组和本地导出状态面板。下面组件代码是早期设计示例，其中 AI 推荐帧和 shadcn slider 均后置，不代表 MVP 必须实现。

### 关键帧选择器组件
```typescript
// components/KeyframeSelector.tsx
'use client';

import { useState, useRef, useEffect } from 'react';
import { Slider } from '@/components/ui/slider';

interface KeyframeSelectorProps {
  videoUrl: string;
  recommendations: Array<{ timestamp: number; score: number }>;
  onSelect: (timestamp: number) => void;
}

export function KeyframeSelector({
  videoUrl,
  recommendations,
  onSelect,
}: KeyframeSelectorProps) {
  const videoRef = useRef<HTMLVideoElement>(null);
  const [currentTime, setCurrentTime] = useState(0);
  const [duration, setDuration] = useState(0);

  useEffect(() => {
    const video = videoRef.current;
    if (!video) return;

    video.addEventListener('loadedmetadata', () => {
      setDuration(video.duration);
    });
  }, []);

  const handleSliderChange = (value: number[]) => {
    const time = value[0];
    setCurrentTime(time);
    if (videoRef.current) {
      videoRef.current.currentTime = time;
    }
  };

  return (
    <div className="space-y-4">
      {/* 视频播放器 */}
      <video
        ref={videoRef}
        src={videoUrl}
        className="w-full rounded-lg"
        controls
      />

      {/* AI推荐帧 */}
      <div className="flex gap-2">
        {recommendations.map((rec, idx) => (
          <button
            key={idx}
            onClick={() => {
              setCurrentTime(rec.timestamp);
              if (videoRef.current) {
                videoRef.current.currentTime = rec.timestamp;
              }
            }}
            className="relative overflow-hidden rounded border-2 border-blue-500"
          >
            <img
              src={`/api/thumbnail?url=${videoUrl}&time=${rec.timestamp}`}
              alt={`推荐帧 ${idx + 1}`}
              className="w-24 h-24 object-cover"
            />
            <span className="absolute top-0 right-0 bg-blue-500 text-white px-2 py-1 text-xs">
              推荐 #{idx + 1}
            </span>
          </button>
        ))}
      </div>

      {/* 时间轴滑块 */}
      <div className="space-y-2">
        <Slider
          value={[currentTime]}
          min={0}
          max={duration}
          step={0.1}
          onValueChange={handleSliderChange}
        />
        <p className="text-sm text-gray-600">
          当前时间: {currentTime.toFixed(2)}s / {duration.toFixed(2)}s
        </p>
      </div>

      {/* 确认按钮 */}
      <button
        onClick={() => onSelect(currentTime)}
        className="w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700"
      >
        确认生成Live Photo
      </button>
    </div>
  );
}
```

### 转换进度组件
```typescript
// components/ConversionProgress.tsx
'use client';

import { useEffect, useState } from 'react';

interface ConversionProgressProps {
  conversionId: string;
  onComplete: (downloadUrl: string) => void;
}

export function ConversionProgress({
  conversionId,
  onComplete,
}: ConversionProgressProps) {
  const [status, setStatus] = useState<'processing' | 'completed' | 'failed'>('processing');
  const [progress, setProgress] = useState(0);

  useEffect(() => {
    const interval = setInterval(async () => {
      const res = await fetch(`/api/convert/${conversionId}/status`);
      const data = await res.json();

      setStatus(data.status);

      if (data.status === 'completed') {
        clearInterval(interval);
        onComplete(data.outputUrl);
      } else if (data.status === 'failed') {
        clearInterval(interval);
      } else {
        setProgress((prev) => Math.min(prev + 10, 90));
      }
    }, 1000);

    return () => clearInterval(interval);
  }, [conversionId]);

  return (
    <div className="space-y-4">
      <div className="relative w-full h-4 bg-gray-200 rounded-full overflow-hidden">
        <div
          className="absolute h-full bg-blue-600 transition-all duration-300"
          style={{ width: `${progress}%` }}
        />
      </div>

      <p className="text-center text-sm">
        {status === 'processing' && '正在生成Live Photo...'}
        {status === 'completed' && '生成完成！'}
        {status === 'failed' && '生成失败，请重试'}
      </p>
    </div>
  );
}
```


---

## 部署方案

### 环境变量配置
MVP 前端本地工具只需要前端运行环境。下面数据库、Redis、R2、JWT、Stripe 均为后置或 P1+ 云端配置，不是 MVP 快速开始的必填项。

```bash
# .env.example

# 数据库
DATABASE_URL=postgresql://user:password@localhost:5432/vidlive
REDIS_HOST=localhost
REDIS_PORT=6379

# Cloudflare R2
R2_ENDPOINT=https://xxx.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_key
R2_SECRET_ACCESS_KEY=your_secret
R2_BUCKET_NAME=vidlive-storage
R2_PUBLIC_URL=https://your-domain.r2.dev

# JWT认证
JWT_SECRET=your_random_secret_key

# 文件存储
UPLOAD_DIR=/tmp/uploads
MAX_FILE_SIZE=524288000  # 500MB

# 付费功能（可选）
STRIPE_SECRET_KEY=sk_test_xxx
```

### Docker Compose配置（P1+/P2+）
Docker Compose 用于云端兜底、队列和后置用户系统验证。MVP 本地前端不需要启动 PostgreSQL/Redis。

```yaml
# docker-compose.yml
version: '3.8'

services:
  # PostgreSQL数据库
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: vidlive
      POSTGRES_USER: vidlive
      POSTGRES_PASSWORD: your_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  # Redis缓存/队列
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  # 后端API服务
  api:
    build: ./backend
    depends_on:
      - postgres
      - redis
    environment:
      - DATABASE_URL=postgresql://vidlive:your_password@postgres:5432/vidlive
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - /tmp/uploads:/tmp/uploads
    ports:
      - "3001:3001"

  # Worker进程（视频处理）
  worker:
    build: ./backend
    command: node dist/worker.js
    depends_on:
      - postgres
      - redis
    environment:
      - DATABASE_URL=postgresql://vidlive:your_password@postgres:5432/vidlive
      - REDIS_HOST=redis
    volumes:
      - /tmp/uploads:/tmp/uploads
    deploy:
      replicas: 3  # 3个Worker并发

volumes:
  postgres_data:
  redis_data:
```

### Dockerfile（后端）
```dockerfile
# backend/Dockerfile
FROM node:20-alpine

# 安装系统依赖
RUN apk add --no-cache \
    ffmpeg \
    perl \
    perl-image-exiftool \
    libheif \
    opencv-dev

WORKDIR /app

RUN corepack enable

# 安装Node依赖
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY apps/api/package.json apps/api/package.json
COPY packages/shared/package.json packages/shared/package.json
RUN pnpm install --frozen-lockfile --prod

# 复制代码
COPY . .

# 构建TypeScript
RUN pnpm --filter @vidlive/shared run build && pnpm --filter @vidlive/api run build

EXPOSE 3001

CMD ["pnpm", "--filter", "@vidlive/api", "start"]
```

### Nginx反向代理配置
```nginx
# /etc/nginx/sites-available/vidlive
upstream api_backend {
    server localhost:3001;
}

server {
    listen 80;
    server_name vidlive.com;

    # 重定向到HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name vidlive.com;

    ssl_certificate /etc/letsencrypt/live/vidlive.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vidlive.com/privkey.pem;

    # 文件上传大小限制
    client_max_body_size 500M;

    # API代理
    location /api/ {
        proxy_pass http://api_backend/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
    }

    # 静态文件（Next.js前端由Vercel托管）
}
```


---

## 性能优化方案

### 1. 视频处理优化
```typescript
// 使用硬件加速（如果GPU可用）
const ffmpegCmd = `ffmpeg -hwaccel cuda -i "${input}" ...`;

// 多线程编码
const ffmpegCmd = `ffmpeg -i "${input}" -threads 4 -preset ultrafast ...`;

// 预设质量档位
const presets = {
  fast: { preset: 'ultrafast', crf: 28 },
  balanced: { preset: 'fast', crf: 23 },
  quality: { preset: 'medium', crf: 18 },
};
```

### 2. 缓存策略（AI 后置）
```typescript
// Redis缓存AI推荐结果（避免重复分析）
const cacheKey = `recommendations:${videoHash}`;
const cached = await redis.get(cacheKey);

if (cached) {
  return JSON.parse(cached);
}

const recommendations = await detector.recommendKeyframes(videoPath);
await redis.set(cacheKey, JSON.stringify(recommendations), 'EX', 3600);
```

### 3. CDN加速（P1+）
- MVP：不上传原始视频，只有前端静态资源走 Vercel/Next.js 托管。
- P1/Beta：云端兜底产物可先走本地临时下载链接。
- P1+：确认云端处理频率和成本后，再接 Cloudflare R2 签名链接；原始素材不做公开 CDN 分发。

### 4. 数据库索引优化（P2/P3+）
```sql
-- 用户查询历史记录
CREATE INDEX idx_conversions_user_created 
ON conversions(user_id, created_at DESC);

-- 清理过期文件定时任务
CREATE INDEX idx_conversions_expires 
ON conversions(expires_at) 
WHERE status = 'completed';
```

---

## 监控与告警

### 日志收集
```typescript
// 使用Pino结构化日志
import pino from 'pino';

const logger = pino({
  level: process.env.LOG_LEVEL || 'info',
  transport: {
    target: 'pino-pretty',
  },
});

// 业务日志
logger.info({
  action: 'conversion_started',
  conversionId,
  userId,
  videoDuration,
});
```

### 性能指标监控
```typescript
// Prometheus指标
import { Counter, Histogram, register } from 'prom-client';

const conversionCounter = new Counter({
  name: 'vidlive_conversions_total',
  help: 'Total number of conversions',
  labelNames: ['status'],
});

const processingDuration = new Histogram({
  name: 'vidlive_processing_duration_seconds',
  help: 'Video processing duration',
  buckets: [1, 5, 10, 30, 60],
});

// 使用
conversionCounter.inc({ status: 'completed' });
processingDuration.observe(duration);
```

### 错误告警（可选集成）
- Sentry：捕获前后端异常
- PagerDuty：关键错误通知
- Grafana：可视化Dashboard

---

## 安全措施

### 1. 文件验证
```typescript
// 检查真实文件类型（防止伪造扩展名）
import fileType from 'file-type';

async function validateVideo(filePath: string): Promise<boolean> {
  const type = await fileType.fromFile(filePath);
  
  const allowedTypes = ['video/mp4', 'video/quicktime', 'video/x-msvideo'];
  
  if (!type || !allowedTypes.includes(type.mime)) {
    throw new Error('Invalid video format');
  }
  
  return true;
}
```

### 2. 速率限制
```typescript
// 使用fastify-rate-limit
import rateLimit from '@fastify/rate-limit';

fastify.register(rateLimit, {
  max: 10, // 10次/分钟
  timeWindow: '1 minute',
  keyGenerator: (req) => req.user?.id || req.ip,
});
```

### 3. 内容审核（可选）
```typescript
// 集成阿里云内容安全
import AliyunGreen from '@alicloud/green20220302';

async function moderateVideo(videoPath: string): Promise<boolean> {
  const client = new AliyunGreen({ /* config */ });
  const result = await client.videoModeration({ url: videoPath });
  
  if (result.suggestion === 'block') {
    throw new Error('Content violates community guidelines');
  }
  
  return true;
}
```


---

## 开发路线图

### Phase 0: 技术与兼容验证
**目标**：证明 Live Photo 生成、保存路径、锁屏播放和隐私边界可验证。

- [ ] FFmpeg/ffprobe/exiftool 环境验证。
- [ ] Phase 0 POC API 生成 JPEG/MOV/manifest/ZIP。
- [ ] 至少一条 iPhone 保存路径跑通。
- [ ] iOS 17+ 锁屏播放记录。
- [ ] 本地模式抓包或网络日志确认不上传素材。

**验收标准**：不是“能生成 ZIP”，而是有真机保存、锁屏播放、抓包/日志记录。没有真机记录就别说 Live Photo 已经稳了。

---

### Phase 1: MVP 本地闭环（2-4周）
**目标**：无登录、本地优先地跑通导入、裁剪、手动关键帧和导出包。

- [ ] **前端本地处理**
  - Next.js App Router 工具页。
  - MP4/MOV/GIF 导入和元信息展示。
  - 时间轴裁剪、手动关键帧、基础画面参数。
  - 本地 ZIP 导出：动态片段、封面、manifest、保存指引。

- [ ] **隐私和失败处理**
  - 本地模式明确“不上传素材”。
  - 不支持格式、解析失败、本地转码失败有用户可读提示。
  - 超过本地能力时提示切换云端，但不自动上传。

- [ ] **保存指引**
  - iPhone Safari、文件 App、Shortcuts、桌面 AirDrop 路径说明。
  - 标准 Live Photo 和锁屏预设风险提示。

**不包含**：用户系统、数据库、支付、AI 关键帧、R2、Redis。  
**验收标准**：桌面和移动端各跑一次完整本地导出；错误路径有提示；本地模式抓包/日志无上传；真机保存路径记录齐。

---

### Phase 2: P1/Beta 云端兜底（1-2个月）
**目标**：补齐本地处理失败或超限时的云端任务能力。

- [ ] Fastify 云端任务 API：上传、状态、下载、删除。
- [ ] 上传前确认：文件会上传、保留 24 小时、可手动删除。
- [ ] 进程内任务 + `local-temp-link` 先跑通 Beta。
- [ ] Redis/BullMQ 和 R2 签名链接作为 P1+ 增强，不挡 MVP。
- [ ] 云端失败、过期链接、删除后 404、metrics 和结构化日志。

**验收标准**：真实上传、轮询、下载、删除、过期和失败路径都有接口/页面证据。

---

### Phase 3: V1 用户系统与产品化
**目标**：在本地/云端路径稳定后，再补账号、配额、历史和更完整的产品能力。

- [ ] 用户系统：注册/登录、JWT、账号资料。
- [ ] 数据库表：users、conversions、usage_logs。
- [ ] 配额、历史记录、兼容反馈汇总。
- [ ] AI 关键帧作为可选增强：先验证人工评价价值，再决定是否上线。
- [ ] 更完整的基础编辑和 WebP/GIF 输出。

**验收标准**：账号、配额、历史、反馈和监控均有自动检查和手动记录。

---

### Phase 4: 商业化验证
**目标**：Pro 权益和付费路径可验证，而不是提前把支付塞进 MVP。

- [ ] Stripe 或等价支付 provider。
- [ ] 订阅计划、取消/退款路径。
- [ ] 批量处理、4K 输出、历史增强。
- [ ] 成本监控、免费转付费指标。

---

### Phase 5: 扩展生态（6个月+）
**目标**：生态构建。

- [ ] 移动端 APP（React Native）。
- [ ] 浏览器插件（Chrome/Safari）。
- [ ] API 开放平台。
- [ ] 模板市场。
- [ ] AI 视频增强（超分辨率）。
- [ ] 社交平台直传集成。

---

## 技术债务与风险

### 已知限制
1. **Live Photo格式兼容性**
   - 风险：苹果未公开标准，可能因系统更新失效
   - 缓解：持续监控iOS新版本，社区跟进

2. **视频处理性能**
   - 风险：大文件处理慢，用户体验差
   - 缓解：硬件加速、预设质量档位、提示预估时间

3. **存储成本**
   - 风险：用户上传大量视频，R2费用攀升
   - 缓解：MVP 默认本地处理；P1+ 云端启用严格清理策略、临时链接和付费用户分摊

### 技术选型风险
- **FFmpeg依赖**：需要系统级安装，Docker部署必须
- **OpenCV性能**：AI 关键帧后置；Node.js版性能一般，后续可能需要 Python/服务端任务重写
- **Cloudflare R2**：P1+ 才接入；签名链接、删除、过期和成本都需要真实验证

---

## iPhone 保存路径与真机兼容矩阵

### 保存路径口径

| 路径 | 适用场景 | 验证要求 |
| --- | --- | --- |
| iPhone Safari 下载 ZIP | 移动端直接导出 | 记录浏览器、iOS 版本、文件 App/下载目录行为；确认用户能找到导出包。 |
| Shortcuts 导入 JPEG/MOV | 尝试写入相册 Live Photo | 记录快捷指令步骤、是否被相册识别为实况照片、失败提示。 |
| 桌面下载 + AirDrop | 桌面生成后传到 iPhone | 记录 macOS/Windows 浏览器、AirDrop/数据线/文件 App 路径、相册识别结果。 |
| 锁屏壁纸设置 | iOS 17+ 锁屏播放 | 使用 1-2 秒、9:16、较少剧烈运动样例；记录是否播放、失败机型和系统版本。 |

### 真机兼容矩阵模板

| 设备 | iOS 版本 | 输入样例 | 保存路径 | 标准 Live Photo 识别 | 锁屏播放 | 失败原因/备注 |
| --- | --- | --- | --- | --- | --- | --- |
| iPhone 1 | 待填 | 待填 | Safari / 文件 App | 待测 | 待测 | 需覆盖 iOS 17+。 |
| iPhone 2 | 待填 | 待填 | Shortcuts | 待测 | 待测 | 需覆盖另一个主要版本。 |
| iPhone 3 | 待填 | 待填 | 桌面下载 + AirDrop | 待测 | 待测 | 记录保存链路和失败条件。 |

通过口径：至少 3 台 iPhone，覆盖 iOS 17+ 两个主要版本；标准预设和锁屏预设都要记录成功和失败样例。没有真机矩阵，不允许把 Live Photo 兼容性写成“已完成”。

---

## 关键技术验证清单

### Phase 0 / MVP 前必须验证：

1. **Live Photo格式可行性**
```bash
# 手动生成一个测试文件，确认iPhone能识别
ffmpeg -i test.mp4 -ss 1.5 -t 3 -c:v libx264 output.mov
exiftool -QuickTime:ContentIdentifier="$(uuidgen)" output.mov
# 传到iPhone验证
```

2. **FFmpeg性能基准**
```bash
# 测试不同preset的速度与质量
time ffmpeg -i input.mp4 -preset ultrafast -crf 23 out1.mov
time ffmpeg -i input.mp4 -preset fast -crf 23 out2.mov
```

3. **本地模式隐私边界**
```text
Chrome DevTools / 代理抓包 / CDP Network 至少一种：
- 本地导入 MP4/MOV/GIF。
- 本地抓帧和导出 ZIP。
- 确认无原始素材、缩略图、导出文件上传请求。
```

4. **移动端和桌面端流程**
```text
- 桌面 Chrome/Edge：导入 -> 裁剪 -> 生成 -> 下载 ZIP。
- iPhone Safari：导入 -> 裁剪 -> 生成 -> 找到下载文件。
- 记录截图/录屏、浏览器版本、素材 ID。
```

5. **OpenCV清晰度算法准确性（AI 后置）**
```python
# 对比10个视频的推荐帧与人工选择
accuracy = correct_frames / total_frames
# 目标：>70%
```

6. **R2上传/下载速度（P1+）**
```typescript
// 测试500MB文件传输时间
// 目标：上传<60s，下载<30s
```

---

## 成本估算（MVP阶段）

| 项目                | 配置                  | 月费用      |
| ------------------- | --------------------- | ----------- |
| 前端托管            | Vercel / 静态托管     | ¥0（免费额度）|
| 后端服务            | MVP 非必需；Phase 0/P1 POC 可用本机或轻量 VPS | ¥0-200 |
| PostgreSQL          | MVP 后置              | ¥0          |
| Redis               | MVP 后置              | ¥0          |
| Cloudflare R2       | MVP 后置，P1+ 再接    | ¥0          |
| 域名 + SSL证书      | Let's Encrypt免费     | ¥50/年      |
| **合计**            |                       | **¥0-200/月** |

**P1+/扩展阶段**（云端兜底和 1000+ 用户）：
- 服务器升级至8核16G：¥500/月
- R2存储100GB：¥15/月
- Redis/PostgreSQL 托管或自建运维成本另计
- CDN流量费：按量计费
- **预估：¥600-800/月**

---

## 附录：依赖包清单

### 后端（Node.js）
```json
{
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/multipart": "^9.0.0",
    "@aws-sdk/client-s3": "^3.1061.0",
    "@aws-sdk/s3-request-presigner": "^3.1061.0",
    "bullmq": "^5.78.0",
    "ioredis": "^5.10.1",
    "dotenv": "^16.4.0",
    "@vidlive/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0"
  }
}
```

### 前端（Next.js）
```json
{
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-dropzone": "^14.2.3",
    "lucide-react": "^0.468.0",
    "qrcode": "^1.5.4",
    "@vidlive/shared": "workspace:*"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
```

---

**文档版本**：v1.1  
**最后更新**：2026年6月4日  
**维护者**：开发团队

---

## 快速开始（开发环境搭建）

```bash
# 1. 克隆仓库（假设）
git clone https://github.com/your-org/vidlive.git
cd vidlive

# 2. 安装 Node/pnpm，并按需安装系统依赖
# MVP 前端本地导出不要求本机 exiftool；Phase 0/P1 云端验证需要 ffmpeg/ffprobe/exiftool。
pnpm -v

# 3. 安装项目依赖
pnpm install

# 4. 配置环境变量
cp .env.example .env

# 5. 启动 MVP 前端本地工具
pnpm run dev:web

# 6. 启动 P1/Beta 云端 API（只在验证云端兜底时需要）
pnpm run dev:api

# 访问 http://localhost:3000
```

数据库、Prisma 迁移、Redis 和 R2 配置后置到 Phase 3/P1+；不要在 MVP 快速开始里默认要求它们。

---

## 参考资料

- [FFmpeg官方文档](https://ffmpeg.org/documentation.html)
- [Live Photo逆向工程](https://github.com/mzp/LoveLiver)
- [OpenCV.js教程](https://docs.opencv.org/4.x/d5/d10/tutorial_js_root.html)
- [BullMQ使用指南](https://docs.bullmq.io/)
- [Cloudflare R2文档](https://developers.cloudflare.com/r2/)
