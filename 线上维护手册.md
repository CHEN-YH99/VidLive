# VidLive 线上维护手册

本文档用于后续维护 `VidLive`：更新功能、修复 bug、部署上线、查看日志、备份数据库、回滚版本、排查常见问题。

写给以后操作的人看：按步骤来，别凭感觉乱敲。线上环境不是练习场，手一抖就开始表演。

维护原则：

- 代码放 GitHub
- 线上密钥和数据库备份私下保存，不提交 Git
- 线上更新按本文档执行
- 后续加功能或修 bug，先本地改代码、检查、提交，再服务器拉取部署

## 1. 当前线上环境

### 1.1 访问地址

在哪里看：浏览器。

```text
https://vidlive.cc.cd
```

### 1.2 服务器信息

在哪里做：本地电脑终端。

```bash
ssh -p 18911 root@你的服务器公网IP
```

说明：

- 服务器系统：`OpenCloudOS 9`
- SSH 端口：`18911`
- 线上项目目录：`/opt/vidlive`
- 不要把服务器公网 IP、Cloudflare token、Resend key、数据库密码发到聊天窗口或提交到 Git。

### 1.3 线上架构

```text
手机 / 电脑浏览器
  ↓
https://vidlive.cc.cd
  ↓
Cloudflare Edge / Universal SSL
  ↓
Cloudflare Tunnel
  ↓
腾讯云服务器 cloudflared
  ↓
http://localhost:8000
  ↓
Docker gateway
  ├─ /api/*  -> backend:8000
  └─ 其他    -> web:3000
```

### 1.4 Docker 服务

在哪里做：服务器。

```bash
cd /opt/vidlive
docker compose -f docker-compose.prod.yml --env-file .env.production ps
```

正常应该看到这些服务：

```text
vidlive-postgres
vidlive-redis
vidlive-backend
vidlive-web
vidlive-gateway
```

`migrate` 是一次性迁移服务，跑完退出是正常的。

## 2. 账号和密钥清单

这些东西只记录“在哪里找”，不要写进 Git。

| 类型 | 用途 | 在哪里 |
| --- | --- | --- |
| 腾讯云账号 | 管服务器、安全组、重装系统 | 腾讯云控制台 |
| GitHub 仓库权限 | 拉代码、推代码 | GitHub |
| Cloudflare 账号 | DNS、Tunnel、证书 | Cloudflare Dashboard |
| Resend API Key | 发送邮箱验证码 | Resend 控制台 |
| `.env.production` | 线上环境变量 | 服务器 `/opt/vidlive/.env.production` |
| `JWT_SECRET` | 登录会话签名 | `.env.production` |
| `POSTGRES_PASSWORD` | 数据库密码 | `.env.production` |

检查线上环境变量时不要直接打印完整密钥。用下面这种方式：

在哪里做：服务器。

```bash
cd /opt/vidlive
docker compose -f docker-compose.prod.yml --env-file .env.production exec backend sh -lc 'for v in NODE_ENV CORS_ORIGIN AUTH_COOKIE_SECURE EMAIL_CODE_FROM EMAIL_CODE_LOG_ENABLED RESEND_API_KEY EMAIL_CODE_SMTP_HOST EMAIL_CODE_WEBHOOK_URL; do val=$(printenv "$v"); if [ -n "$val" ]; then case "$v" in RESEND_API_KEY) echo "$v=<set>";; *) echo "$v=$val";; esac; else echo "$v=<empty>"; fi; done'
```

## 3. 本地开发准备

### 3.1 进入本地项目目录

在哪里做：本地 Windows 电脑终端。

```bash
cd /d E:\个人项目\VidLive
```

如果你用的是 PowerShell：

```powershell
Set-Location "E:\个人项目\VidLive"
```

### 3.2 安装依赖

在哪里做：本地项目目录。

```bash
pnpm install
```

如果提示没有 `pnpm`：

```bash
corepack enable
corepack prepare pnpm@10.33.0 --activate
pnpm --version
```

### 3.3 启动本地开发

前端：

```bash
pnpm run dev:web
```

后端：

```bash
pnpm run dev:api
```

说明：

- 前端是 `apps/web`
- 后端是 `apps/api`
- 共享代码是 `packages/shared`
- 改公共类型或工具时，先考虑是不是应该放在 `packages/shared`

## 4. 修 bug / 加功能的标准流程

以后不管是修 bug 还是加功能，都按这个流程。

### 4.1 先更新本地代码

在哪里做：本地项目目录。

```bash
git status --short
git pull
```

如果 `git status --short` 有你没确认的本地改动，先看清楚再 `pull`。别一把梭，梭完冲突就开始怀疑人生。

### 4.2 新建分支

在哪里做：本地项目目录。

修 bug：

```bash
git switch -c fix/问题简述
```

加功能：

```bash
git switch -c feat/功能简述
```

示例：

```bash
git switch -c fix/email-code-empty-body
git switch -c feat/r2-direct-upload
```

### 4.3 修改代码的位置

常见需求对应位置：

| 需求 | 优先看哪里 |
| --- | --- |
| 页面样式、按钮、交互 | `apps/web` |
| API 接口、登录注册、验证码 | `apps/api/src/modules/v1` |
| 视频转换、上传、下载 | `apps/api/src/modules/conversions` |
| 前后端共用类型 | `packages/shared` |
| Docker 生产部署 | `docker-compose.prod.yml` |
| Nginx 网关转发 | `deploy/nginx/default.conf` |
| 生产环境变量模板 | `.env.docker.example` |

### 4.4 本地检查

在哪里做：本地项目目录。

先跑类型检查：

```bash
pnpm run types
```

跑 lint：

```bash
pnpm run lint
```

跑测试：

```bash
pnpm run test
```

如果只改了后端接口，可以额外跑：

```bash
pnpm --filter @vidlive/api run types
pnpm --filter @vidlive/api run test
pnpm --filter @vidlive/api run build
```

如果只改了前端页面，可以额外跑：

```bash
pnpm --filter @vidlive/web run types
pnpm --filter @vidlive/web run build
```

完整构建：

```bash
pnpm run build
```

最低上线门槛：

```text
pnpm run types 通过
pnpm run lint 通过
相关测试通过
相关包 build 通过
```

别跳过检查。跳过检查上线，等于闭眼开车下坡，还嫌刹车碍事。

### 4.5 提交代码

在哪里做：本地项目目录。

```bash
git status --short
git add 你修改的文件
git commit -m "fix: 修复验证码请求异常"
```

示例：

```bash
git add apps/api/src/modules/v1/v1.routes.ts apps/api/src/modules/v1/v1.routes.test.ts
git commit -m "fix: handle malformed auth request bodies"
```

推到 GitHub：

```bash
git push origin 当前分支名
```

如果你直接维护主分支，确认本地检查都通过后：

```bash
git switch main
git pull
git merge 当前分支名
git push origin main
```

## 5. 上线更新流程

### 5.1 登录服务器

在哪里做：本地电脑终端。

```bash
ssh -p 18911 root@你的服务器公网IP
```

### 5.2 进入项目目录

在哪里做：服务器。

```bash
cd /opt/vidlive
```

确认当前目录：

```bash
pwd
ls
```

应该看到：

```text
/opt/vidlive
package.json
docker-compose.prod.yml
.env.docker.example
apps
deploy
packages
```

### 5.3 拉取最新代码

在哪里做：服务器 `/opt/vidlive`。

```bash
git status --short
git pull
```

如果服务器上出现本地改动，先不要硬拉。先看改了什么：

```bash
git diff
```

生产服务器原则上不要直接改源码。要改就本地改、提交 GitHub、服务器 `git pull`。

### 5.4 重新构建并启动

在哪里做：服务器 `/opt/vidlive`。

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
```

查看状态：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production ps
```

### 5.5 验证本机入口

在哪里做：服务器。

```bash
curl http://127.0.0.1:8000/api/health
```

预期类似：

```json
{"status":"ok"}
```

### 5.6 验证公网入口

在哪里做：本地电脑或服务器都可以。

```bash
curl https://vidlive.cc.cd/api/health
```

预期类似：

```json
{"status":"ok"}
```

### 5.7 浏览器验证

在哪里做：电脑浏览器和手机浏览器。

打开：

```text
https://vidlive.cc.cd
```

至少验证：

- 页面能打开
- 注册能发送验证码
- 邮箱能收到验证码
- 注册能完成
- 登录能成功
- 上传一个小于 100 MB 的小视频能进入转换流程

Cloudflare 免费 / Pro 通常不要按 500 MB 上传去测试。大文件要么升级套餐，要么做分片上传或 R2 直传。不然 413 报错不是玄学，是你硬撞墙。

## 6. 常用运维命令

所有命令默认在哪里做：服务器 `/opt/vidlive`。

先进入目录：

```bash
cd /opt/vidlive
```

### 6.1 查看服务状态

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production ps
```

### 6.2 查看后端日志

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f backend
```

### 6.3 查看前端日志

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f web
```

### 6.4 查看 gateway 日志

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f gateway
```

### 6.5 查看数据库日志

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f postgres
```

### 6.6 查看 Redis 日志

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f redis
```

### 6.7 重启某个服务

重启后端：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production restart backend
```

重启前端：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production restart web
```

重启 gateway：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production restart gateway
```

### 6.8 重启全部服务

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production restart
```

### 6.9 停止全部服务

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production down
```

注意：`down` 不会默认删除 Docker volume，但线上别乱加 `-v`。`-v` 会删数据卷，数据库和上传文件都可能没了，扑街得非常彻底。

### 6.10 启动全部服务

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d
```

### 6.11 查看容器资源占用

```bash
docker stats
```

退出 `docker stats`：

```text
Ctrl + C
```

### 6.12 查看磁盘空间

```bash
df -h
```

查看 Docker 占用：

```bash
docker system df
```

清理未使用镜像和缓存：

```bash
docker system prune -f
```

不要随便执行：

```bash
docker system prune -a --volumes
```

这条会更激进，可能清到你不想清的东西。

## 7. Cloudflare Tunnel 维护

### 7.1 查看 cloudflared 状态

在哪里做：服务器。

```bash
systemctl status cloudflared
```

正常应该是：

```text
active (running)
```

### 7.2 查看 Tunnel 日志

在哪里做：服务器。

```bash
journalctl -u cloudflared -f
```

### 7.3 重启 Tunnel

在哪里做：服务器。

```bash
systemctl restart cloudflared
systemctl status cloudflared
```

### 7.4 Cloudflare 控制台配置

在哪里做：Cloudflare Zero Trust 控制台。

路径大致是：

```text
Cloudflare Dashboard
  -> Zero Trust
  -> Networks
  -> Tunnels
  -> vidlive 对应 Tunnel
  -> Public Hostnames
```

当前应该配置为：

```text
Hostname: vidlive.cc.cd
Type: HTTP
URL: localhost:8000
```

不要填 `backend:8000`。Cloudflare Tunnel 跑在服务器系统层，不在 Docker 网络里，填 `backend:8000` 它不认识。

## 8. 邮箱验证码维护

### 8.1 测试验证码发送

在哪里做：服务器或本地电脑。

```bash
curl -sS -X POST 'https://vidlive.cc.cd/api/v1/auth/email-codes' \
  -H 'content-type: application/json' \
  -d '{"email":"你的邮箱@example.com","purpose":"register","username":"TestUser"}'
```

正常返回应该包含：

```json
{
  "ok": true,
  "purpose": "register",
  "delivery": "resend",
  "message": "验证码已发送，请在 10 分钟内完成验证。"
}
```

### 8.2 检查 Resend 配置

在哪里做：服务器 `/opt/vidlive`。

```bash
cd /opt/vidlive
docker compose -f docker-compose.prod.yml --env-file .env.production exec backend sh -lc 'for v in EMAIL_CODE_FROM EMAIL_CODE_LOG_ENABLED RESEND_API_KEY RESEND_API_URL RESEND_TIMEOUT_MS; do val=$(printenv "$v"); if [ -n "$val" ]; then case "$v" in RESEND_API_KEY) echo "$v=<set>";; *) echo "$v=$val";; esac; else echo "$v=<empty>"; fi; done'
```

重点：

- `RESEND_API_KEY=<set>` 才代表配置了 key
- `EMAIL_CODE_FROM` 要是已验证的发信域名或邮箱
- 生产环境 `EMAIL_CODE_LOG_ENABLED=false`

### 8.3 修改邮件配置

在哪里做：服务器。

```bash
cd /opt/vidlive
nano .env.production
```

常用配置：

```dotenv
EMAIL_CODE_FROM="VidLive <noreply@vidlive.cc.cd>"
RESEND_API_KEY=你的ResendKey
RESEND_API_URL=https://api.resend.com/emails
RESEND_TIMEOUT_MS=10000
EMAIL_CODE_LOG_ENABLED=false
```

保存后重启后端：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend
```

再测试：

```bash
curl -sS -X POST 'https://vidlive.cc.cd/api/v1/auth/email-codes' \
  -H 'content-type: application/json' \
  -d '{"email":"你的邮箱@example.com","purpose":"register","username":"TestUser"}'
```

## 9. 环境变量维护

### 9.1 编辑线上环境变量

在哪里做：服务器。

```bash
cd /opt/vidlive
nano .env.production
```

关键项：

```dotenv
WEB_PORT=8000
BACKEND_PORT=3010
NODE_ENV=production
UPLOAD_DIR=/app/uploads

MAX_LOCAL_FILE_SIZE=104857600
MAX_CLOUD_FILE_SIZE=104857600

POSTGRES_DB=video_prompt
POSTGRES_USER=postgres
POSTGRES_PASSWORD=强密码
DATABASE_URL=postgresql://postgres:强密码@postgres:5432/video_prompt?schema=public
REDIS_URL=redis://redis:6379

JWT_SECRET=至少32字符随机字符串
AUTH_COOKIE_SECURE=true
CORS_ORIGIN=https://vidlive.cc.cd

VITE_API_BASE_URL=/api
NEXT_PUBLIC_API_BASE_URL=/api
```

### 9.2 生成随机密钥

在哪里做：服务器。

```bash
openssl rand -hex 32
```

把输出填到：

```dotenv
JWT_SECRET=这里填随机字符串
```

注意：改 `JWT_SECRET` 会让已有登录会话失效，用户需要重新登录。

### 9.3 修改环境变量后重启

如果只改后端相关变量：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend
```

如果改了前端构建变量，例如 `NEXT_PUBLIC_API_BASE_URL`：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build web gateway
```

如果不确定影响范围：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
```

## 10. 数据库备份和恢复

### 10.1 创建备份

在哪里做：服务器 `/opt/vidlive`。

```bash
cd /opt/vidlive
mkdir -p backups
docker compose -f docker-compose.prod.yml --env-file .env.production exec -T postgres pg_dump -U postgres video_prompt > backups/vidlive_$(date +%Y%m%d_%H%M%S).sql
ls -lh backups
```

建议：

- 每次上线前备份一次
- 每次改数据库结构前备份一次
- 定期把备份下载到本地或对象存储

### 10.2 下载备份到本地

在哪里做：本地电脑终端。

```bash
scp -P 18911 root@你的服务器公网IP:/opt/vidlive/backups/备份文件名.sql .
```

注意：`ssh` 是小写 `-p`，`scp` 是大写 `-P`。这个设计很烦，但它就是这样。

### 10.3 恢复数据库

高危操作。先确认你真的要恢复，先备份当前库。

在哪里做：服务器 `/opt/vidlive`。

先备份当前库：

```bash
cd /opt/vidlive
mkdir -p backups
docker compose -f docker-compose.prod.yml --env-file .env.production exec -T postgres pg_dump -U postgres video_prompt > backups/before_restore_$(date +%Y%m%d_%H%M%S).sql
```

停止业务服务：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production stop backend web gateway
```

导入备份：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production exec -T postgres psql -U postgres -d video_prompt < backups/你的备份文件.sql
```

重新启动：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d
```

验证：

```bash
curl http://127.0.0.1:8000/api/health
curl https://vidlive.cc.cd/api/health
```

说明：上面是同结构数据导入流程。如果要“彻底回到某个备份状态”，可能需要先清库再导入，这属于更高危操作，别在没确认备份可用时乱搞。

### 10.4 自动备份

仓库提供了自动备份脚本：

```text
deploy/maintenance/backup-production.sh
```

它会做两件事：

- 备份 `/opt/vidlive/.env.production`
- 备份 PostgreSQL 数据库并压缩成 `.sql.gz`

默认保留最近 30 天备份，目录是：

```text
/opt/vidlive/backups
```

在哪里做：服务器 `/opt/vidlive`。

先给脚本执行权限：

```bash
cd /opt/vidlive
chmod +x deploy/maintenance/*.sh
```

手动跑一次：

```bash
./deploy/maintenance/backup-production.sh
ls -lh backups
```

如果要改保留天数，例如保留 14 天：

```bash
RETENTION_DAYS=14 ./deploy/maintenance/backup-production.sh
```

安装定时任务：

```bash
./deploy/maintenance/install-cron.sh
```

默认定时任务：

```text
每天 03:10 自动备份
每 10 分钟执行一次健康检查
```

查看定时任务：

```bash
crontab -l
```

查看自动备份日志：

```bash
tail -f logs/maintenance/backup.log
```

下载自动备份到本地电脑：

```bash
scp -P 18911 root@你的服务器公网IP:/opt/vidlive/backups/备份文件名.sql.gz .
scp -P 18911 root@你的服务器公网IP:/opt/vidlive/backups/env.production.备份时间.bak .
```

注意：`env.production.*.bak` 是敏感文件，里面有密钥和密码。不要提交 GitHub，不要截图，不要发群里。

### 10.5 健康检查脚本

仓库提供了健康检查脚本：

```text
deploy/maintenance/health-check.sh
```

它会检查：

- Docker Compose 服务状态
- 本机 `http://127.0.0.1:8000/api/health`
- 公网 `https://vidlive.cc.cd/api/health`
- `cloudflared` 是否 active
- 根分区磁盘占用是否超过阈值

手动执行：

```bash
cd /opt/vidlive
./deploy/maintenance/health-check.sh
```

查看健康检查日志：

```bash
tail -f logs/maintenance/health.log
```

如果要把磁盘报警阈值改成 90%：

```bash
DISK_WARN_PERCENT=90 ./deploy/maintenance/health-check.sh
```

卸载自动定时任务：

```bash
crontab -l | grep -v 'deploy/maintenance/backup-production.sh' | grep -v 'deploy/maintenance/health-check.sh' | crontab -
```

## 11. 回滚版本

### 11.1 推荐方式：用 `git revert`

适合已经推到主分支的错误提交。

在哪里做：本地项目目录。

```bash
git log --oneline -5
git revert 出问题的commit
pnpm run types
pnpm run lint
pnpm run test
git push origin main
```

然后服务器上线：

```bash
ssh -p 18911 root@你的服务器公网IP
cd /opt/vidlive
git pull
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
curl http://127.0.0.1:8000/api/health
curl https://vidlive.cc.cd/api/health
```

### 11.2 临时救火方式：服务器切到旧 commit

只适合紧急恢复，后面要回到正常 Git 流程。

在哪里做：服务器 `/opt/vidlive`。

```bash
cd /opt/vidlive
git log --oneline -10
git checkout 旧commit
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
curl http://127.0.0.1:8000/api/health
curl https://vidlive.cc.cd/api/health
```

注意：这样会进入 detached HEAD。救火可以，长期这样维护就是给自己挖坑。

恢复到主分支：

```bash
git switch main
git pull
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
```

## 12. 安全加固

### 12.1 gateway 只监听本机

当前仓库已经把 `docker-compose.prod.yml` 里的 `gateway` 改成只监听本机：

```yaml
ports:
  - "127.0.0.1:${WEB_PORT:-8000}:80"
```

这样做的作用：

- 服务器公网不能直接扫到 `8000`
- 只有本机 `cloudflared` 能访问 `localhost:8000`
- Cloudflare Tunnel 仍然正常工作

服务器拉取这次更新后，执行：

```bash
cd /opt/vidlive
git pull
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build gateway
curl http://127.0.0.1:8000/api/health
curl https://vidlive.cc.cd/api/health
```

### 12.2 腾讯云安全组

在哪里做：腾讯云控制台。

入站建议：

```text
TCP 18911  来源：你的电脑公网IP/32
```

不需要开放：

```text
22
80
443
8000
3010
5432
6379
```

出站建议：

```text
允许 443/tcp
允许 Cloudflare Tunnel 所需出站连接
```

大多数腾讯云默认出站放行，别没事乱收紧到把 Tunnel 自己干掉。

### 12.3 不要提交这些文件

```text
.env
.env.local
.env.production
backups/
server-backups/
*.sql
*.log
```

提交前看一眼：

```bash
git status --short
```

## 13. 常见故障排查

### 13.1 页面打不开

先在本地电脑测：

```bash
curl https://vidlive.cc.cd/api/health
```

再登录服务器测：

```bash
ssh -p 18911 root@你的服务器公网IP
cd /opt/vidlive
curl http://127.0.0.1:8000/api/health
```

判断：

- 本机 `127.0.0.1:8000` 正常，公网不正常：看 Cloudflare Tunnel / DNS
- 本机也不正常：看 Docker 服务

### 13.2 Docker 服务异常

在哪里做：服务器。

```bash
cd /opt/vidlive
docker compose -f docker-compose.prod.yml --env-file .env.production ps
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f backend
```

重点看：

- `backend` 是否 healthy
- `web` 是否 healthy
- `gateway` 是否启动
- `postgres` 是否 healthy
- `redis` 是否 healthy

### 13.3 Tunnel inactive

在哪里做：服务器。

```bash
systemctl status cloudflared
journalctl -u cloudflared -f
```

尝试重启：

```bash
systemctl restart cloudflared
systemctl status cloudflared
```

如果还不行，去 Cloudflare 控制台看 Tunnel 的 connector 是否 `Healthy`。

### 13.4 注册收不到验证码

先测接口：

```bash
curl -sS -X POST 'https://vidlive.cc.cd/api/v1/auth/email-codes' \
  -H 'content-type: application/json' \
  -d '{"email":"你的邮箱@example.com","purpose":"register","username":"TestUser"}'
```

看返回：

- `delivery":"resend"`：后端已经调用 Resend，去邮箱垃圾箱或 Resend 控制台查投递
- `email-code-delivery-not-configured`：邮件服务没配
- `internal-server-error`：看后端日志

后端日志：

```bash
cd /opt/vidlive
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f backend
```

### 13.5 登录返回 400

先确认不是接口挂了：

```bash
curl https://vidlive.cc.cd/api/health
```

登录 400 常见原因：

- 邮箱或用户名不存在
- 密码错误
- 验证码错误或过期
- 请求字段不符合后端校验

看后端日志：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f backend
```

如果 `/api/v1/auth/session`、`/api/v1/auth/challenge` 是 200，只有 `/api/v1/auth/login` 是 400，大概率是登录输入问题，不是 Cloudflare 或 Docker 问题。

### 13.6 上传报 413

原因通常是请求体太大。

检查位置：

```text
Cloudflare 上传限制
deploy/nginx/default.conf 的 client_max_body_size
.env.production 的 MAX_CLOUD_FILE_SIZE
```

Cloudflare 免费 / Pro 前面拦 100 MB 级别上传时，项目后端设 500 MB 也没用。前门都过不去，后厨再大也白搭。

建议生产设置：

```dotenv
MAX_CLOUD_FILE_SIZE=104857600
```

然后重启后端：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend
```

### 13.7 后端数据库连接失败

看日志：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f backend
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f postgres
```

检查环境变量：

```bash
grep -E 'POSTGRES_DB|POSTGRES_USER|DATABASE_URL' .env.production
```

注意：不要把 `POSTGRES_PASSWORD` 直接截图发出去。

常见坑：

- `POSTGRES_PASSWORD` 和 `DATABASE_URL` 里的密码不一致
- 密码里有 `@`、`:`、`/`、`?`，但 `DATABASE_URL` 没有 URL encode
- `postgres` 容器没 healthy

### 13.8 磁盘满

在哪里做：服务器。

```bash
df -h
docker system df
```

清理 Docker 构建缓存和无用镜像：

```bash
docker system prune -f
```

查看备份目录：

```bash
du -sh /opt/vidlive/backups
ls -lh /opt/vidlive/backups
```

删除旧备份前先确认已经下载或不再需要。删库跑路不是维护，是事故。

## 14. 日常维护清单

### 每次上线前

在哪里做：本地项目目录。

```bash
pnpm run types
pnpm run lint
pnpm run test
```

在哪里做：服务器。

```bash
cd /opt/vidlive
mkdir -p backups
docker compose -f docker-compose.prod.yml --env-file .env.production exec -T postgres pg_dump -U postgres video_prompt > backups/before_deploy_$(date +%Y%m%d_%H%M%S).sql
```

### 每次上线后

在哪里做：服务器和浏览器。

```bash
curl http://127.0.0.1:8000/api/health
curl https://vidlive.cc.cd/api/health
docker compose -f docker-compose.prod.yml --env-file .env.production ps
```

浏览器验证：

```text
https://vidlive.cc.cd
```

至少测：

- 首页打开
- 注册
- 邮箱验证码
- 登录
- 一个小文件上传转换

### 每周检查

在哪里做：服务器。

```bash
df -h
docker system df
docker compose -f docker-compose.prod.yml --env-file .env.production ps
systemctl status cloudflared
```

### 每月检查

在哪里做：Cloudflare、Resend、腾讯云控制台。

检查：

- Cloudflare Tunnel 是否 Healthy
- DNS 是否仍指向 Cloudflare
- Resend 是否有投递失败或额度问题
- 腾讯云磁盘、CPU、内存是否够用
- 是否需要下载数据库备份到本地保存

## 15. 一键更新命令备忘

确认本地已经提交并推送到 GitHub 后，服务器执行：

```bash
ssh -p 18911 root@你的服务器公网IP
cd /opt/vidlive
git pull
docker compose -f docker-compose.prod.yml --env-file .env.production up -d --build
docker compose -f docker-compose.prod.yml --env-file .env.production ps
curl http://127.0.0.1:8000/api/health
curl https://vidlive.cc.cd/api/health
```

如果这里任何一步失败，停下来查日志，不要继续乱敲：

```bash
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f backend
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f web
docker compose -f docker-compose.prod.yml --env-file .env.production logs -f gateway
journalctl -u cloudflared -f
```
