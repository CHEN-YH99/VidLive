# VidLive 真机验收与抓包操作教程

## 文档目的

本文档用于补齐当前闯关文档中仍为 `[阻塞]` 或 `[部分完成]` 的人工证据。核心目标不是再写一堆功能，而是把真实设备、真实浏览器、抓包、性能和云端环境的证据补上。

当前本地 CDP 已验证：

- Chrome Headless 桌面和移动视口无横向溢出。
- GIF/MP4 本地导出均出现“下载 ZIP 包”。
- 本地模式无变更类请求、无 `/api/` 请求。

但 CDP 不等于 iPhone Safari，不等于真机相册，也不等于 HAR/代理抓包。别拿模拟器当真机证据，容易被现实当场教育。

---

## 0. 证据命名和保存规范

建议所有证据放在本地 `evidence/` 目录，不提交原始用户素材。

推荐目录：

```text
evidence/
  2026-06-04/
    devices/
    safari/
    har/
    performance/
    cloud/
```

推荐命名：

```text
iphone15-ios17.5-local-mp4-screenrecord.mov
iphone13-ios16.7-local-gif-result.png
chrome-local-mp4-no-upload.har
iphone-safari-proxy-local-gif-no-upload.har
perf-10s-1080p-run-01.txt
cloud-consent-before-upload.png
```

每条证据至少记录：

| 字段 | 示例 |
| --- | --- |
| 日期 | 2026-06-04 |
| 操作人 | 用户 / Codex 协助 |
| 设备 | iPhone 15 Pro |
| 系统 | iOS 17.5 |
| 浏览器 | Safari / Chrome / Edge |
| 素材 | MP4 10s 1080p / GIF 1s |
| 操作路径 | 上传、裁剪、选帧、导出、下载、保存 |
| 结果 | 成功 / 失败 |
| 失败提示 | 页面提示、系统提示、控制台错误 |
| 附件 | 截图、录屏、HAR、日志 |

---

## 1. Docker 与 exiftool 容器验证

### 当前状态

Docker Desktop 已启动，API 检查镜像已构建通过。

已验证：

| 工具 | 容器版本 |
| --- | --- |
| exiftool | 13.55 |
| ffmpeg | 8.0.1 |
| ffprobe | 8.0.1 |

### 你需要做

后续重新验证前，只需要确认 Docker Desktop 处于 Engine running，并使用 Linux containers。

### 可重复验证命令

```powershell
docker context ls
docker build -f apps/api/Dockerfile -t vidlive-api-exiftool-check .
docker run --rm vidlive-api-exiftool-check exiftool -ver
```

### 通过标准

| 检查 | 通过标准 |
| --- | --- |
| Docker engine | `docker info` 可返回 Server 信息 |
| API 镜像构建 | `docker build` 成功 |
| exiftool | `docker run ... exiftool -ver` 输出版本号 |
| 文档记录 | 将版本、命令、输出写回闯关文档 |

---

## 2. 三台 iPhone 真机矩阵

### 你需要准备

至少 3 台 iPhone，建议覆盖：

| 设备编号 | 建议设备 | iOS 要求 | 必测原因 |
| --- | --- | --- | --- |
| D1 | 较新机型，如 iPhone 15/16 | iOS 17+ | 锁屏动态壁纸主验收 |
| D2 | 中间机型，如 iPhone 13/14 | iOS 16/17 | 普通用户覆盖 |
| D3 | 较老机型，如 iPhone 11/12 | iOS 15/16/17 | 兼容风险覆盖 |

记录表：

| 设备编号 | 机型 | iOS 版本 | Safari 版本 | 可用空间 | 网络 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| D1 |  |  |  |  | Wi-Fi |  |
| D2 |  |  |  |  | Wi-Fi |  |
| D3 |  |  |  |  | Wi-Fi |  |

### 每台设备必测路径

1. iPhone Safari 打开 VidLive 页面。
2. 上传 MP4。
3. 上传 GIF。
4. 修改时间轴起点、终点。
5. 修改关键帧。
6. 切换标准 Live Photo 预设。
7. 切换 iOS 锁屏壁纸预设。
8. 点击生成文件。
9. 下载 ZIP。
10. 在文件 App 中打开 ZIP。
11. 检查是否包含 README、manifest、封面、动态片段或兜底素材。
12. 尝试保存到相册或通过 Shortcuts/AirDrop 路径处理。
13. 尝试设置为锁屏壁纸。

### 结果记录表

| 设备 | 素材 | 预设 | 导入 | 元信息 | 裁剪 | 关键帧 | 导出 ZIP | 文件 App 打开 | 相册识别 | 锁屏播放 | 失败说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D1 | MP4 | 标准 |  |  |  |  |  |  |  |  |  |
| D1 | MP4 | 锁屏 |  |  |  |  |  |  |  |  |  |
| D1 | GIF | 标准 |  |  |  |  |  |  |  |  |  |
| D2 | MP4 | 标准 |  |  |  |  |  |  |  |  |  |
| D2 | GIF | 标准 |  |  |  |  |  |  |  |  |  |
| D3 | MP4 | 标准 |  |  |  |  |  |  |  |  |  |
| D3 | GIF | 标准 |  |  |  |  |  |  |  |  |  |

### 通过标准

| 项目 | 通过标准 |
| --- | --- |
| 导入 | Safari 能选择素材，页面显示文件名、大小、时长或尺寸 |
| 裁剪 | 起点/终点变化后导出仍完成 |
| 关键帧 | 关键帧预览或 manifest 中记录正确 |
| 导出 | 出现“导出结果”和“下载 ZIP 包” |
| 错误路径 | 不支持文件出现“格式不支持”或等价提示 |
| 真机保存 | 记录成功路径或明确失败原因 |
| 锁屏播放 | iOS 17+ 设备记录是否能播放 |

---

## 3. 本地服务和 iPhone Safari 访问

### 你需要做

1. 电脑和 iPhone 连接同一个 Wi-Fi。
2. 暂时关闭 VPN、代理、iCloud Private Relay 等会影响局域网访问的功能。
3. 允许 Windows 防火墙放行 Node.js。

### 我会启动本地服务

先获取电脑局域网 IP：

```powershell
ipconfig
```

找到类似：

```text
IPv4 Address . . . . . . . . . . . : 192.168.1.23
```

启动 Web：

```powershell
pnpm --filter @vidlive/shared run build
pnpm --filter @vidlive/web run dev -- -H 0.0.0.0 -p 3000
```

iPhone Safari 打开：

```text
http://192.168.1.23:3000
```

如果要测云端 API，再启动 API：

```powershell
$env:API_HOST='0.0.0.0'
$env:API_PORT='3001'
$env:CORS_ORIGIN='http://192.168.1.23:3000'
pnpm --filter @vidlive/api run dev
```

Web 需要指向局域网 API 时：

```powershell
$env:NEXT_PUBLIC_API_BASE_URL='http://192.168.1.23:3001'
pnpm --filter @vidlive/web run dev -- -H 0.0.0.0 -p 3000
```

### 常见问题

| 现象 | 处理 |
| --- | --- |
| iPhone 打不开页面 | 确认同 Wi-Fi、IP 正确、防火墙放行 Node |
| 页面能打开但上传按钮异常 | 换 Safari，关闭内容拦截插件 |
| 下载没有反应 | 查看 Safari 下载列表和文件 App 下载目录 |
| ZIP 打不开 | 截图系统提示，记录文件大小 |
| 生成失败 | 截图页面错误提示，保留素材信息 |

---

## 4. 隐私抓包和上传验证

隐私验收分两类：本地模式不上传，云端模式确认后才上传。

### 4.1 桌面 Chrome HAR

你或我执行：

1. 打开 Chrome。
2. 打开 DevTools。
3. 进入 Network。
4. 勾选 Preserve log。
5. 勾选 Disable cache。
6. 清空已有请求。
7. 打开 VidLive 页面。
8. 本地模式上传 MP4。
9. 裁剪、选关键帧、生成 ZIP。
10. 上传 GIF 再跑一遍。
11. 右键 Network 列表，选择 Save all as HAR with content。

HAR 文件命名：

```text
chrome-local-mp4-gif-no-upload.har
```

本地模式通过标准：

| 检查 | 通过标准 |
| --- | --- |
| 原始素材 | 不出现上传原始文件的 POST/PUT/PATCH/DELETE |
| 缩略图/封面 | 不出现上传图片请求 |
| API | 不出现 `/api/` 处理请求 |
| 第三方 | 不出现可疑外部上传域名 |

### 4.2 iPhone Safari 代理抓包

推荐工具任选一个：

- Proxyman
- Charles
- Fiddler
- mitmproxy

操作思路：

1. 电脑启动代理工具，监听端口，例如 `8888`。
2. iPhone 和电脑连接同一 Wi-Fi。
3. iPhone 进入 Wi-Fi 设置。
4. 点击当前 Wi-Fi 的信息按钮。
5. HTTP Proxy 选择 Manual。
6. Server 填电脑局域网 IP。
7. Port 填代理端口。
8. 打开 iPhone Safari 访问 VidLive。
9. 跑本地 MP4/GIF 导出。
10. 导出代理会话记录。

如果抓 HTTPS 云端请求，需要安装并信任代理证书；本地 HTTP 页面一般不需要证书。

iPhone 代理通过标准和 Chrome HAR 一样：本地模式不应上传原始素材、缩略图或导出文件。

### 4.3 云端上传确认抓包

云端模式要验证两个路径：

| 路径 | 预期 |
| --- | --- |
| 未勾选上传确认 | 不允许上传，不应出现真实上传请求 |
| 勾选上传确认 | 允许上传，出现明确的上传 API 请求 |

记录表：

| 环境 | 是否勾选确认 | 是否出现上传请求 | 请求方法 | 请求路径 | 结果 | 附件 |
| --- | --- | --- | --- | --- | --- | --- |
| Chrome | 否 |  |  |  |  |  |
| Chrome | 是 |  |  |  |  |  |
| iPhone Safari | 否 |  |  |  |  |  |
| iPhone Safari | 是 |  |  |  |  |  |

---

## 5. 性能 P75 验收

### 5.1 样例素材

可以使用真实素材，也可以由我用 FFmpeg 生成合成素材。不要使用包含隐私内容的视频。

生成目录：

```powershell
New-Item -ItemType Directory -Force tmp\perf-samples
```

10 秒 1080p：

```powershell
ffmpeg -y -f lavfi -i testsrc2=size=1920x1080:rate=30 -t 10 -c:v libx264 -pix_fmt yuv420p tmp\perf-samples\perf-10s-1080p.mp4
```

接近 100MB 样例：

```powershell
ffmpeg -y -f lavfi -i testsrc2=size=1920x1080:rate=30 -t 15 -c:v libx264 -b:v 55M -maxrate 55M -bufsize 110M -pix_fmt yuv420p tmp\perf-samples\perf-near-100mb.mp4
```

30 秒 1080p 云端样例：

```powershell
ffmpeg -y -f lavfi -i testsrc2=size=1920x1080:rate=30 -t 30 -c:v libx264 -pix_fmt yuv420p tmp\perf-samples\perf-30s-1080p.mp4
```

### 5.2 本地性能记录

每个样例至少跑 5 次，建议 10 次。记录从“选择文件后页面开始解析”到“元信息出现”的耗时，以及从“点击生成文件”到“下载 ZIP 包出现”的耗时。

记录表：

| 样例 | 轮次 | 浏览器 | 设备 | 解析耗时秒 | 导出耗时秒 | 成功/失败 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 10s 1080p | 1 | Chrome | Windows |  |  |  |  |
| 10s 1080p | 2 | Chrome | Windows |  |  |  |  |
| near 100MB | 1 | Chrome | Windows |  |  |  |  |
| near 100MB | 2 | Chrome | Windows |  |  |  |  |

P75 计算：

1. 将耗时从小到大排序。
2. 5 次样例取第 4 个值。
3. 10 次样例取第 8 个值。

当前目标：

| 项目 | 目标 |
| --- | --- |
| 首屏可交互 | 小于 3 秒 |
| 本地解析 100MB 内视频 | P75 小于 10 秒 |
| 本地转换 10 秒 1080p | P75 小于 60 秒 |
| 导出包生成 | P75 小于 10 秒 |
| 云端转换 30 秒 1080p | P75 小于 30 秒 |

---

## 6. P1/Beta、R2、Redis、Stripe 准备

这一节不是 MVP 必须项。除非明确推进 P1/Beta 或商业化，否则不要拿它挡 MVP 真机验收。先把本地闭环验真，再开大工程，不然就像饭没煮熟先买十套餐具。

### 6.1 本地 Redis/Postgres

当前 `docker-compose.yml` 只提供 Postgres 和 Redis。

启动：

```powershell
docker compose up -d
docker compose ps
```

关闭：

```powershell
docker compose down
```

需要记录：

| 服务 | 通过标准 |
| --- | --- |
| Postgres | 容器 healthy 或端口 5432 可连接 |
| Redis | 容器 running，端口 6379 可连接 |
| API | 能读取 `DATABASE_URL` 和 `REDIS_URL` |

### 6.2 云端 local-temp-link 验证

如果只验 Beta 兜底，不接 R2，也可以先用本地临时链接。

建议执行：

```powershell
pnpm run smoke:phase2
```

需要补的人工证据：

| 场景 | 预期 |
| --- | --- |
| 小文件上传 | 创建任务成功 |
| 状态轮询 | pending/processing/done 状态可见 |
| 下载 ZIP | 可下载 |
| 删除任务 | 删除后下载 404 |
| 过期任务 | 缩短保留时间后 expired 或下载失效 |
| 未确认上传 | 不发生上传 |

### 6.3 R2 真实签名链接

需要你提供测试环境配置，不要提交到 Git：

```text
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_SIGNED_URL_TTL_SECONDS=3600
```

通过标准：

| 检查 | 通过标准 |
| --- | --- |
| 上传 | 产物进入 R2 bucket |
| 签名链接 | 浏览器可下载 |
| 过期 | TTL 后链接失效 |
| 删除 | 删除后链接立即失效 |
| 隐私 | 原始素材不公开分发 |

### 6.4 Stripe 或支付服务

支付不是当前 MVP 通关条件。若要推进，需要你提供测试环境：

```text
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO_MONTHLY=
```

必须验证：

| 场景 | 预期 |
| --- | --- |
| 测试卡升级 Pro | 用户权益变为 Pro |
| 取消订阅 | 下周期或立即降级逻辑明确 |
| 退款 | 权益处理明确 |
| webhook 重放 | 不重复发权益 |
| 发票/失败付款 | 页面提示明确 |

---

## 7. 最终写回规则

每完成一组证据，更新：

1. `开发闯关文档.md`
2. 对应矩阵记录文档
3. 必要时更新 `技术实现文档.md`

不得只口头说“测过了”。每个 `[完成]` 必须有：

- 操作环境
- 操作步骤
- 结果截图/录屏/HAR/日志
- 失败路径记录
- 日期和操作者

这套规则麻烦，但省命。项目不是靠感觉上线，是靠证据通关。
